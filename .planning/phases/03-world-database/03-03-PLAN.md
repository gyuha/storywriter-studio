---
phase: 3
plan: 3
title: 타임라인 + 캐릭터 관계 (백엔드 + 프론트엔드)
status: planned
requirements: [WORLD-04, WORLD-05, WORLD-06]
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - apps/api/src/domains/world/repository/timeline_repository.py
  - apps/api/src/domains/world/repository/relationship_repository.py
  - apps/api/src/domains/world/service/timeline_service.py
  - apps/api/src/domains/world/service/relationship_service.py
  - apps/api/src/domains/world/router/timeline_router.py
  - apps/api/src/domains/world/router/relationship_router.py
  - apps/api/src/domains/world/router/world_router.py
  - apps/web/src/features/world/types/world.ts
  - apps/web/src/features/world/lib/world-api.ts
  - apps/web/src/features/world/hooks/use-world-queries.ts
  - apps/web/src/features/world/hooks/use-world-mutations.ts
  - apps/web/src/features/world/components/timeline-list.tsx
  - apps/web/src/features/world/components/timeline-form-modal.tsx
  - apps/web/src/features/world/components/relationship-list.tsx
  - apps/web/src/features/world/components/relationship-form-modal.tsx
  - apps/web/src/features/world/components/character-list.tsx
  - apps/web/src/features/world/components/world-page.tsx
autonomous: true

must_haves:
  truths:
    - "POST /api/v1/novels/{novel_id}/timelines가 201을 반환하고, chapter_id가 nullable로 처리된다"
    - "GET /api/v1/novels/{novel_id}/characters/{id}/relationships가 direction 필드를 포함하여 반환한다"
    - "캐릭터 탭 내 관계 섹션에서 선택된 캐릭터의 관계 목록이 표시된다"
    - "시간표 탭에서 항목 추가 시 챕터 선택 드롭다운이 제공된다"
    - "캐릭터 관계 생성 시 direction: source로 응답이 반환된다"
  artifacts:
    - path: "apps/api/src/domains/world/repository/relationship_repository.py"
      provides: "OR 쿼리로 character_id_a OR character_id_b 양방향 조회"
    - path: "apps/api/src/domains/world/service/relationship_service.py"
      provides: "_to_relationship_response로 direction 계산"
    - path: "apps/api/src/domains/world/router/timeline_router.py"
      provides: "타임라인 CRUD 5개 엔드포인트"
    - path: "apps/api/src/domains/world/router/relationship_router.py"
      provides: "캐릭터 관계 CRUD 엔드포인트"
    - path: "apps/web/src/features/world/components/timeline-list.tsx"
      provides: "시간표 탭 UI (챕터 연결 포함)"
    - path: "apps/web/src/features/world/components/relationship-list.tsx"
      provides: "캐릭터 탭 내 관계 서브섹션"
  key_links:
    - from: "relationship_service.py"
      to: "RelationshipResponse"
      via: "_to_relationship_response (direction 계산)"
    - from: "relationship_repository.py"
      to: "CharacterRelationship"
      via: "or_(character_id_a == id, character_id_b == id)"
    - from: "timeline-form-modal.tsx"
      to: "useChapters (novel)"
      via: "챕터 목록 조회 후 select 옵션으로 제공"
---

## Objective

Plan 1에서 ORM 모델과 마이그레이션은 이미 작성된 상태다. 이 Plan은 타임라인과 캐릭터 관계의 repository·service·router를 완성하고, 프론트엔드에 시간표 탭과 캐릭터 관계 서브섹션을 추가한다.

Purpose: WORLD-04(시간표), WORLD-05(인간관계) 요구사항을 완성하여 Phase 3 전체 Success Criteria를 달성한다.
Output: 타임라인 5개 + 관계 4개 엔드포인트, HeyAPI 재생성, 4번째 탭(시간표) + 캐릭터 관계 서브섹션

## Tasks

### Task 1: 타임라인 + 캐릭터 관계 백엔드

**Action:**

아날로그: Plan 1 Task 2에서 구현한 character_repository/service/router 패턴 그대로 적용.

**1. `timeline_repository.py` 작성:**
- `get_by_id(novel_id, timeline_id)`, `list_by_novel(novel_id)`, `create(novel_id, **fields)`, `update(timeline, **fields)`, `delete(timeline)`
- 기본 패턴은 `character_repository.py`와 동일. 특이사항 없음.
- `chapter_id`는 nullable FK — repository는 그냥 저장/조회, 검증은 서비스에서.

**2. `relationship_repository.py` 작성 (RESEARCH.md §3.1 OR 쿼리 패턴 적용):**
```python
from sqlalchemy import or_

async def list_by_character(
    self, novel_id: uuid.UUID, character_id: uuid.UUID
) -> list[CharacterRelationship]:
    stmt = (
        select(CharacterRelationship)
        .where(
            CharacterRelationship.novel_id == novel_id,
            or_(
                CharacterRelationship.character_id_a == character_id,
                CharacterRelationship.character_id_b == character_id,
            ),
        )
        .order_by(CharacterRelationship.created_at.asc())
    )
    result = await self.session.execute(stmt)
    return list(result.scalars().all())

async def get_by_id(self, novel_id: uuid.UUID, rel_id: uuid.UUID) -> CharacterRelationship | None:
    result = await self.session.execute(
        select(CharacterRelationship).where(
            CharacterRelationship.id == rel_id,
            CharacterRelationship.novel_id == novel_id,
        )
    )
    return result.scalar_one_or_none()
```
+ `create(novel_id, character_id_a, character_id_b, **fields)`, `update(rel, **fields)`, `delete(rel)`

**3. `timeline_service.py` 작성:**
- `novel_repo: NovelRepository` + `timeline_repo: TimelineRepository` 주입
- `_verify_novel_ownership` 패턴
- `list_timelines`, `get_timeline`, `create_timeline`, `update_timeline`, `delete_timeline`
- `create_timeline` 시 `chapter_id` 검증: 전달된 경우 챕터가 같은 novel에 속하는지 확인할 필요는 없음 (MVP). nullable FK이므로 DB 레벨에서 자동 처리.

**4. `relationship_service.py` 작성 (RESEARCH.md §3.2 direction 계산 패턴):**
- `novel_repo: NovelRepository`, `character_repo: CharacterRepository`, `rel_repo: RelationshipRepository` 주입
- `_verify_novel_ownership` 패턴
- `_to_relationship_response(rel: CharacterRelationship, character_id: uuid.UUID) -> RelationshipResponse`:
  ```python
  is_source = rel.character_id_a == character_id
  return RelationshipResponse(
      id=rel.id,
      novel_id=rel.novel_id,
      character_id_a=rel.character_id_a,
      character_id_b=rel.character_id_b,
      type=rel.type,
      description=rel.description,
      created_at=rel.created_at,
      updated_at=rel.updated_at,
      direction="source" if is_source else "target",
      other_character_id=rel.character_id_b if is_source else rel.character_id_a,
  )
  ```
  `RelationshipResponse`는 `from_attributes=False`이므로 수동 생성 필수 (Pitfall 3 회피).
- `list_relationships(novel_id, user_id, character_id) -> list[RelationshipResponse]`
- `create_relationship(novel_id, user_id, character_id_a, data: RelationshipCreate) -> RelationshipResponse`
- `update_relationship(novel_id, user_id, rel_id, character_id, data: RelationshipUpdate) -> RelationshipResponse`
- `delete_relationship(novel_id, user_id, rel_id) -> None`

**5. `timeline_router.py` 작성 (D-11 엔드포인트):**
```python
router = APIRouter(tags=["timelines"])
# GET/POST /novels/{novel_id}/timelines
# GET/PUT/DELETE /novels/{novel_id}/timelines/{timeline_id}
```
`_get_timeline_service`: `TimelineService(NovelRepository(session), TimelineRepository(session))`

**6. `relationship_router.py` 작성 (D-12 엔드포인트):**
```python
router = APIRouter(tags=["relationships"])
# POST /novels/{novel_id}/characters/{character_id}/relationships → 201
# GET  /novels/{novel_id}/characters/{character_id}/relationships → list[RelationshipResponse]
# PUT  /novels/{novel_id}/characters/{character_id}/relationships/{rel_id}
# DELETE /novels/{novel_id}/characters/{character_id}/relationships/{rel_id} → 204
```
`_get_relationship_service`: `RelationshipService(NovelRepository(session), CharacterRepository(session), RelationshipRepository(session))`

**7. `world_router.py` 수정 — timeline + relationship 라우터 추가:**
```python
from .timeline_router import router as timeline_router
from .relationship_router import router as relationship_router

router.include_router(timeline_router)
router.include_router(relationship_router)
```

**8. HeyAPI 재생성:**
백엔드 서버 실행 후:
```bash
pnpm --filter storywriter-studio-web run generate:api
```
생성된 `sdk.gen.ts`에 timeline, relationship 관련 함수 포함 여부 확인:
```bash
grep -c "Timelines\|Relationships" apps/web/src/generated/sdk.gen.ts
```

**Verify:**
```bash
curl -s http://localhost:8000/openapi.json | python3 -c "
import sys, json
paths = json.load(sys.stdin)['paths']
timeline_paths = [p for p in paths if 'timelines' in p]
rel_paths = [p for p in paths if 'relationships' in p]
print(f'Timelines: {len(timeline_paths)}, Relationships: {len(rel_paths)}')
"
```
`Timelines: 2, Relationships: 2` (또는 그 이상) 출력 시 성공.

**Done when:** 타임라인 5개 + 관계 4개 엔드포인트가 `/docs`에 등록되고, `sdk.gen.ts`에 해당 함수가 생성된다.

---

### Task 2: 시간표 탭 + 캐릭터 관계 서브섹션 프론트엔드

**Action:**

**1. `features/world/types/world.ts` 추가:**
Plan 2에서 작성한 파일에 타임라인·관계 타입을 추가한다:
```typescript
export interface Timeline {
  id: string; novel_id: string; event_name: string;
  event_date: string | null; description: string | null;
  chapter_id: string | null; summary: string | null;
  created_at: string; updated_at: string;
}
export interface TimelineCreateInput { event_name: string; event_date?: string; description?: string; chapter_id?: string; summary?: string }
export interface TimelineUpdateInput { event_name?: string; event_date?: string; description?: string; chapter_id?: string; summary?: string }

export type RelationshipType = 'lover' | 'enemy' | 'ally' | 'family';
export interface Relationship {
  id: string; novel_id: string;
  character_id_a: string; character_id_b: string;
  type: RelationshipType; description: string | null;
  direction: 'source' | 'target'; other_character_id: string;
  created_at: string; updated_at: string;
}
export interface RelationshipCreateInput { character_id_b: string; type: RelationshipType; description?: string }
export interface RelationshipUpdateInput { type?: RelationshipType; description?: string }
```

**2. `features/world/lib/world-api.ts` 추가:**
`sdk.gen.ts`에 생성된 실제 함수명을 사용하여:
- `apiGetTimelines(novelId)`, `apiCreateTimeline(novelId, data)`, `apiUpdateTimeline(novelId, timelineId, data)`, `apiDeleteTimeline(novelId, timelineId)`
- `apiGetRelationships(novelId, characterId)`, `apiCreateRelationship(novelId, characterId, data)`, `apiUpdateRelationship(novelId, characterId, relId, data)`, `apiDeleteRelationship(novelId, characterId, relId)`

**3. `features/world/hooks/use-world-queries.ts` 추가:**
```typescript
export function useTimelines(novelId: string) {
  return useQuery({
    queryKey: ['worlds', novelId, 'timelines'],
    queryFn: () => apiGetTimelines(novelId),
    enabled: !!novelId,
  });
}
export function useRelationships(novelId: string, characterId: string) {
  return useQuery({
    queryKey: ['worlds', novelId, 'relationships', characterId],
    queryFn: () => apiGetRelationships(novelId, characterId),
    enabled: !!novelId && !!characterId,
  });
}
```

**4. `features/world/hooks/use-world-mutations.ts` 추가:**
타임라인 Create/Update/Delete mutation (토스트 메시지: `'시간표 항목이 ...'`)
관계 Create/Update/Delete mutation (토스트 메시지: `'관계가 ...'`)
관계 mutation의 `invalidateQueries`: `['worlds', novelId, 'relationships', characterId]`

**5. `timeline-form-modal.tsx` 작성:**
- Props: `{ open: boolean; onClose: () => void; novelId: string; timeline?: Timeline }`
- 필드: `event_name`(필수), `event_date`(optional, 자유 텍스트), `description`(optional), `chapter_id`(optional select)
- `chapter_id` select: `useChapters(novelId)` 훅으로 챕터 목록 조회 — `features/novel/hooks/use-novel-queries.ts`의 `useChapters` 또는 기존 챕터 조회 훅 사용. 없으면 `apiGetChapters`를 직접 호출해 novel-api.ts 패턴으로 구현.
- select 옵션: `[{ value: '', label: '챕터 없음' }, ...chapters.map(c => ({ value: c.id, label: c.title }))]`
- zod 스키마: `event_name: z.string().min(1)`, `event_date/description: z.string().optional()`, `chapter_id: z.string().optional()`
- 제출 시 `chapter_id: ''` → `undefined`로 변환하여 API에 전달

**6. `timeline-list.tsx` 작성:**
- Props: `{ novelId: string }`
- `useTimelines(novelId)` 호출
- 테이블 컬럼: 사건명, 날짜(없으면 '-'), 연결 챕터(없으면 '-'), 설명 요약, 수정/삭제 버튼
- 삭제: `window.confirm` 후 `useDeleteTimelineMutation.mutate`

**7. `relationship-form-modal.tsx` 작성:**
- Props: `{ open: boolean; onClose: () => void; novelId: string; characterId: string; relationship?: Relationship }`
- 필드: `character_id_b`(select — 같은 소설의 다른 캐릭터 목록, `useCharacters(novelId)` 사용), `type`(select), `description`(optional)
- `character_id_b` select에서 현재 characterId는 제외: `characters.filter(c => c.id !== characterId)`
- type select 옵션: `lover: '연인'`, `enemy: '적대'`, `ally: '동료'`, `family: '가족'`
- zod: `character_id_b: z.string().min(1, '상대 캐릭터를 선택해주세요')`, `type: z.enum([...])`, `description: z.string().optional()`

**8. `relationship-list.tsx` 작성:**
- Props: `{ novelId: string; characterId: string }`
- `useRelationships(novelId, characterId)` 호출
- 테이블 컬럼: 관계 방향(direction === 'source' ? '→' : '←'), 상대 캐릭터 이름, 관계 유형(한국어), 설명, 수정/삭제 버튼
- 상대 캐릭터 이름 표시: `other_character_id`로 캐릭터 목록(`useCharacters`)에서 조회
- '관계 추가' 버튼으로 `RelationshipFormModal` 열기

**9. `character-list.tsx` 수정 (Plan 2에서 작성):**
- 캐릭터 행을 클릭하거나 '관계' 버튼을 클릭하면 해당 캐릭터의 관계 서브섹션을 표시
- 로컬 state: `selectedCharacterId: string | null`
- `selectedCharacterId`가 있으면 테이블 아래에 `<RelationshipList novelId={novelId} characterId={selectedCharacterId} />`를 렌더링
- '관계' 버튼은 각 행의 액션 컬럼에 추가

**10. `world-page.tsx` 수정 (Plan 2에서 작성):**
- 4번째 탭 '시간표' 추가
- `activeTab` 타입: `'characters' | 'locations' | 'world-settings' | 'timelines'`
- 시간표 탭 패널에 `<TimelineList novelId={novelId} />` 렌더링

**Verify:**
```bash
pnpm --filter storywriter-studio-web run typecheck
```
오류 없음. 그 다음 브라우저에서:
- `/novels/:novelId/world` → 4개 탭 확인
- 시간표 탭 → 추가 버튼 → 챕터 select 드롭다운 표시 확인
- 캐릭터 탭 → 특정 캐릭터의 관계 버튼 클릭 → RelationshipList 표시 확인

**Done when:**
- TypeScript 컴파일 오류 없음
- 시간표 탭에서 항목 추가·수정·삭제 동작
- 캐릭터 탭에서 특정 캐릭터 선택 시 관계 서브섹션이 표시되고 관계 추가·삭제 동작
- 관계 응답의 `direction` 필드가 화살표(→/←)로 표시됨

## Success Criteria

1. `uv run --directory apps/api pytest` (또는 import 검증)에서 timeline, relationship 서비스 오류 없음
2. `GET /api/v1/novels/{novel_id}/characters/{id}/relationships` 응답에 `direction`, `other_character_id` 필드 포함
3. `pnpm --filter storywriter-studio-web run typecheck` 오류 없음
4. 세계관 페이지에 4개 탭(캐릭터·장소·세계관 설정·시간표)이 모두 표시된다
5. 시간표 항목 생성 시 챕터 선택 드롭다운이 제공되고, 선택된 챕터 ID가 저장된다
6. 캐릭터 관계 방향(→/←)이 UI에 표시된다
7. Phase 3 전체 Success Criteria 4개 항목이 모두 달성된다:
   - 캐릭터 CRUD + 관계 정의 (WORLD-01, WORLD-05)
   - 장소·세계관 설정 CRUD (WORLD-02, WORLD-03)
   - 시간표 CRUD + 챕터 연결 (WORLD-04)
   - 이름/유형 검색·필터링 (WORLD-06)
