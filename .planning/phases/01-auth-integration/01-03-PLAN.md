---
phase: 01-auth-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - apps/web/src/features/auth/hooks/use-auth-mutation.ts
  - apps/web/src/features/auth/hooks/use-init-auth.ts
  - apps/web/src/routes/__root.tsx
  - apps/web/src/routes/_authenticated.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "이메일/비밀번호로 로그인하면 실제 FastAPI JWT를 받아 localStorage에 저장하고 인증 상태가 된다"
    - "회원가입 성공 후 자동으로 로그인되어 인증 상태로 전환된다 (signup → 자동 login 연속 호출)"
    - "앱 마운트 시 localStorage에 access_token이 있으면 GET /api/v1/auth/me를 호출하여 세션을 복원한다"
    - "세션 복원 중에는 전체 앱에 로딩 스피너가 표시된다"
    - "인증되지 않은 사용자가 _authenticated 하위 라우트 접근 시 /auth/login으로 리다이렉트된다"
    - "로그인된 사용자가 새로고침해도 세션이 유지된다"
  artifacts:
    - path: "apps/web/src/features/auth/hooks/use-init-auth.ts"
      provides: "앱 마운트 시 세션 복원 훅"
      exports: ["useInitAuth"]
    - path: "apps/web/src/features/auth/hooks/use-auth-mutation.ts"
      provides: "실제 API 기반 login/signup mutation"
    - path: "apps/web/src/routes/_authenticated.tsx"
      provides: "pathless layout route + beforeLoad 가드"
    - path: "apps/web/src/routes/__root.tsx"
      provides: "useInitAuth 호출 + 스피너 렌더링"
  key_links:
    - from: "apps/web/src/routes/_authenticated.tsx"
      to: "localStorage + useAuthStore"
      via: "beforeLoad → localStorage.getItem('access_token')"
      pattern: "localStorage.getItem.*access_token"
    - from: "apps/web/src/routes/__root.tsx"
      to: "apps/web/src/features/auth/hooks/use-init-auth.ts"
      via: "useInitAuth()"
      pattern: "useInitAuth"
    - from: "apps/web/src/features/auth/hooks/use-init-auth.ts"
      to: "/api/v1/auth/me"
      via: "apiGetMe(token)"
      pattern: "apiGetMe"
---

<objective>
프론트엔드 인증 흐름을 실제 백엔드에 완전히 연결한다. mock 코드를 제거하고, 세션 복원(useInitAuth), 실제 로그인/회원가입 mutation, 라우트 가드(_authenticated.tsx)를 구현한다.

Purpose: AUTH-01(로그인), AUTH-02(회원가입+자동로그인), AUTH-03(라우트 보호) 동시 충족.
Output: use-init-auth.ts, 수정된 use-auth-mutation.ts, _authenticated.tsx, 수정된 __root.tsx
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-integration/01-CONTEXT.md
@.planning/phases/01-auth-integration/01-RESEARCH.md
@.planning/phases/01-auth-integration/01-PATTERNS.md
@.planning/phases/01-auth-integration/01-02-SUMMARY.md

<interfaces>
<!-- Plan 02에서 생성된 auth-api.ts exports -->
```typescript
// apps/web/src/features/auth/lib/auth-api.ts
export async function apiLogin(email: string, password: string): Promise<TokenResponse>
export async function apiSignup(email: string, password: string, displayName: string): Promise<void>
export async function apiGetMe(token: string): Promise<AuthUser>
export async function apiLogout(accessToken: string, refreshToken: string): Promise<void>
```

<!-- Plan 02에서 확장된 AuthUser 타입 -->
```typescript
// apps/web/src/features/auth/types/auth.ts
export interface AuthUser { id, email, display_name, is_verified, is_active, created_at }
export interface TokenResponse { access_token, refresh_token, token_type, expires_in }
export interface LoginInput { email, password }
export interface SignupInput { name, email, password, confirmPassword }
```

<!-- useAuthStore — 변경 없음, 사용 패턴 -->
```typescript
// 컴포넌트 내부
const setUser = useAuthStore((state) => state.setUser);
const clearUser = useAuthStore((state) => state.clearUser);
// React 컨텍스트 밖 (beforeLoad)
const { isAuthenticated } = useAuthStore.getState();
```

<!-- 현재 use-auth-mutation.ts (수정 대상) -->
현재 파일: apps/web/src/features/auth/hooks/use-auth-mutation.ts
현재 mutationFn: mockLogin, mockSignup (mock-auth-api.ts 참조 — 이미 삭제됨)
→ apiLogin, apiGetMe, apiSignup 기반으로 교체

<!-- 현재 __root.tsx 구조 -->
RootComponent()에서 useRouterState 호출. AppProviders 래핑. 스피너 없음.
→ useInitAuth() 추가, isInitialized=false 시 스피너 반환
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: use-init-auth.ts 생성 + __root.tsx 수정</name>
  <files>
    apps/web/src/features/auth/hooks/use-init-auth.ts
    apps/web/src/routes/__root.tsx
  </files>
  <read_first>
    - apps/web/src/routes/__root.tsx (현재 파일 전체 — RootComponent 구조, AppProviders 위치, import 목록)
    - apps/web/src/features/auth/hooks/use-auth-mutation.ts (useAuthStore 사용 패턴 확인)
    - apps/web/src/providers/app-providers.tsx (AppProviders가 QueryClientProvider를 포함하는지 확인 — useInitAuth는 순수 fetch 사용이므로 QueryClient 없어도 동작)
  </read_first>
  <action>
**use-init-auth.ts 신규 생성** (per D-08, D-09, D-10, D-11, D-12 — 별도 훅 파일로 분리):

```typescript
import { useEffect, useState } from 'react';
import { apiGetMe } from '../lib/auth-api';
import { useAuthStore } from '../store/auth.store';

export function useInitAuth() {
  const [isInitialized, setIsInitialized] = useState(false);
  const setUser = useAuthStore((state) => state.setUser);
  const clearUser = useAuthStore((state) => state.clearUser);

  useEffect(() => {
    const token = localStorage.getItem('access_token');
    if (!token) {
      setIsInitialized(true);
      return;
    }

    apiGetMe(token)
      .then((user) => {
        setUser(user);  // D-09: /me 성공 → setUser()
      })
      .catch(() => {
        // D-10: /me 실패(401) → localStorage 클리어 + clearUser()
        localStorage.removeItem('access_token');
        localStorage.removeItem('refresh_token');
        clearUser();
      })
      .finally(() => {
        setIsInitialized(true);
      });
  }, []); // 마운트 1회만 실행

  return { isInitialized };
}
```

**__root.tsx 수정** (per D-11, D-12 — PATTERNS.md __root.tsx 섹션 기반):

- `import { useInitAuth } from '@/features/auth/hooks/use-init-auth';` 추가
- RootComponent 내부에 `const { isInitialized } = useInitAuth();` 추가
- `isInitialized`가 false일 때 조기 반환으로 스피너 표시:
  ```tsx
  if (!isInitialized) {
    return (
      <AppProviders>
        <div className="flex h-screen items-center justify-center">
          <div className="h-8 w-8 animate-spin rounded-full border-4 border-primary border-t-transparent" />
        </div>
      </AppProviders>
    );
  }
  ```
- isInitialized=true 이후의 기존 JSX 구조는 변경 없이 유지 (ThemeToggle, Outlet, Modals, Toaster, Devtools)
- **주의:** AppProviders는 스피너 상태에서도 감싸야 함 (per PATTERNS.md __root.tsx 주의사항)
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep "error TS" | grep -E "use-init-auth|__root" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/features/auth/hooks/use-init-auth.ts가 존재하고 `useInitAuth` 함수를 named export한다
    - use-init-auth.ts의 useEffect 의존성 배열이 `[]` (마운트 1회)이다
    - use-init-auth.ts에서 localStorage 토큰이 없으면 즉시 `setIsInitialized(true)`를 호출한다
    - use-init-auth.ts에서 /me 실패 시 `localStorage.removeItem('access_token')`, `localStorage.removeItem('refresh_token')`, `clearUser()`를 호출한다
    - __root.tsx에 `useInitAuth` import와 `isInitialized` 체크가 추가된다
    - `grep -n "isInitialized" apps/web/src/routes/__root.tsx` 결과가 존재한다
    - `grep -n "animate-spin" apps/web/src/routes/__root.tsx` 결과가 존재한다 (로딩 스피너)
    - pnpm typecheck에서 이 파일들 관련 오류 없음
  </acceptance_criteria>
  <done>use-init-auth.ts 생성 완료. __root.tsx에 세션 복원 훅과 스피너 추가 완료.</done>
</task>

<task type="auto">
  <name>Task 2: use-auth-mutation.ts 교체 + _authenticated.tsx 생성</name>
  <files>
    apps/web/src/features/auth/hooks/use-auth-mutation.ts
    apps/web/src/routes/_authenticated.tsx
  </files>
  <read_first>
    - apps/web/src/features/auth/hooks/use-auth-mutation.ts (현재 파일 전체 — mockLogin, mockSignup import 확인, useNavigate 사용 패턴)
    - apps/web/src/routes/auth/login.tsx (createFileRoute 사용 패턴 — _authenticated.tsx 작성 참조)
    - apps/web/src/features/auth/store/auth.store.ts (setUser, clearUser 시그니처 최종 확인)
    - apps/web/src/features/auth/schema/ (zod 스키마 있는 경우 — use-auth-mutation.ts에서 타입 유추하는 곳 확인)
  </read_first>
  <action>
**use-auth-mutation.ts 수정** (per PATTERNS.md use-auth-mutation.ts 섹션):

mock import 제거, 실제 API 함수로 교체:

`useLoginMutation`:
- `mutationFn`: `apiLogin(data.email, data.password)` 호출 → `TokenResponse` 수신 → `localStorage.setItem('access_token', ...)` + `localStorage.setItem('refresh_token', ...)` → `apiGetMe(tokenResponse.access_token)` 호출 → `AuthUser` 반환
- `onSuccess(user)`: `setUser(user)` → `navigate({ to: '/' })`
- `onError`: `toast.error(error instanceof Error ? error.message : '로그인에 실패했습니다')`

`useSignupMutation` (per D-02, AUTH-02 — 자동 login 연속 호출):
- `mutationFn`: `apiSignup(data.email, data.password, data.name)` 호출 (data.name → displayName 매핑, per Pitfall 5) → 성공 시 `apiLogin(data.email, data.password)` 연속 호출 → `localStorage.setItem('access_token', ...)` + `localStorage.setItem('refresh_token', ...)`
- `onSuccess`: `toast.success('가입이 완료되었습니다!')` → `navigate({ to: '/' })`
- `onError`: `toast.error(error instanceof Error ? error.message : '회원가입에 실패했습니다')`

(선택) `useLogoutMutation` — 이미 구현되어 있는 경우 `apiLogout`으로 mutationFn 교체, 없는 경우 새로 추가:
- `mutationFn`: `apiLogout(accessToken, refreshToken)` → `clearUser()` → localStorage 클리어 → `navigate({ to: '/auth/login' })`

**_authenticated.tsx 신규 생성** (per D-04, D-05, PATTERNS.md _authenticated.tsx 섹션):

- `createFileRoute('/_authenticated')` — pathless layout route (언더스코어 접두사)
- `beforeLoad`: localStorage 1차 체크만 (per RESEARCH.md Pitfall 2 권고사항):
  ```typescript
  beforeLoad: () => {
    const token = localStorage.getItem('access_token');
    if (!token) {
      throw redirect({ to: '/auth/login' });
    }
    // isAuthenticated 2차 체크는 useInitAuth() 완료 후 의미 있음
    // 토큰 존재하면 통과 — 실제 401은 API 호출 시 처리
  }
  ```
- component: `() => <Outlet />`

**금지:** `beforeLoad` 내에서 `useAuthStore(...)` 훅 형태 사용. 반드시 `useAuthStore.getState()` 사용 (React 훅 컨텍스트 밖).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep "error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - use-auth-mutation.ts에 `mock-auth-api` import가 없다: `grep "mock-auth-api" apps/web/src/features/auth/hooks/use-auth-mutation.ts` 결과가 없다
    - `useLoginMutation`의 mutationFn이 `apiLogin` → `localStorage.setItem` → `apiGetMe` 순서로 호출한다
    - `useSignupMutation`의 mutationFn이 `apiSignup` 호출 후 `apiLogin`을 연속 호출한다 (AUTH-02)
    - `apiSignup` 호출 시 세 번째 인자가 `data.name`이다 (display_name 매핑)
    - apps/web/src/routes/_authenticated.tsx가 존재한다
    - _authenticated.tsx에 `createFileRoute('/_authenticated')` 선언이 있다
    - _authenticated.tsx의 `beforeLoad`에서 `localStorage.getItem('access_token')` 체크와 `redirect({ to: '/auth/login' })` 호출이 있다
    - pnpm typecheck 오류 없음 (또는 이 파일들과 무관한 오류만 존재)
  </acceptance_criteria>
  <done>use-auth-mutation.ts mock 완전 제거, 실제 API 연결. _authenticated.tsx 생성으로 AUTH-01/02/03 기능 완성.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→localStorage | access_token, refresh_token 저장/읽기 |
| browser→FastAPI | 실제 JWT 교환 및 /me 세션 복원 |
| router→보호 라우트 | beforeLoad 가드 통과 여부 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Spoofing | useInitAuth /me 실패 처리 | mitigate | 401 응답 시 localStorage 완전 클리어 + clearUser() — 만료된 토큰이 잔류하지 않음 |
| T-01-07 | Elevation of Privilege | _authenticated.tsx beforeLoad 경합 | accept | localStorage 1차 체크로 단순화 (per Pitfall 2) — 실제 권한 검증은 각 API 호출에서 백엔드가 처리 |
| T-01-08 | Repudiation | 로그아웃 시 토큰 처리 | mitigate | apiLogout으로 서버 사이드 토큰 무효화 + localStorage 클리어 |
| T-01-SC | Tampering | (해당 없음 — 신규 패키지 설치 없음) | accept | 이 Plan은 신규 패키지를 설치하지 않음 |
</threat_model>

<verification>
```bash
# 타입 검사 전체 통과 확인
cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck
# 기대값: 오류 없음 (0 errors)

# mock import 완전 제거 확인
grep -r "mock-auth-api" apps/web/src/
# 기대값: 결과 없음

# _authenticated.tsx 라우트 등록 확인 (Vite 재시작 후 routeTree.gen.ts 자동 생성됨)
grep "_authenticated" apps/web/src/routes/routeTree.gen.ts
# 기대값: _authenticated 관련 라인 존재

# 개발 서버에서 /auth/login 미인증 접근 확인 (수동)
# 브라우저에서 localhost:5173/some-protected-path → /auth/login으로 리다이렉트되어야 함
```
</verification>

<success_criteria>
- pnpm typecheck 오류 없음
- mock-auth-api 참조가 codebase에서 완전히 제거됨
- useLoginMutation: 실제 FastAPI로 로그인, access_token을 localStorage에 저장
- useSignupMutation: 회원가입 후 자동 로그인 (AUTH-02)
- useInitAuth: localStorage 토큰 존재 시 /me 호출로 세션 복원
- _authenticated.tsx: 토큰 없는 사용자를 /auth/login으로 리다이렉트
- __root.tsx: 초기화 중 로딩 스피너 표시
</success_criteria>

<output>
작업 완료 후 `.planning/phases/01-auth-integration/01-03-SUMMARY.md` 생성
</output>
