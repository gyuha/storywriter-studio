---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/alembic/versions/0007_novel_tagline_tags.py
  - apps/api/src/domains/novel/models/novel_models.py
  - apps/api/src/domains/novel/schemas/novel_schemas.py
  - apps/api/src/domains/novel/router/novel_router.py
  - apps/api/src/domains/novel/service/novel_service.py
  - apps/web/src/features/novel/types/novel.ts
  - apps/web/src/features/novel/components/novel-settings-page.tsx
autonomous: false
requirements:
  - NOVEL-01
  - NOVEL-02
  - NOVEL-03
  - NOVEL-04
  - NOVEL-05
  - NOVEL-06

must_haves:
  truths:
    - "GET /novels/{id} 응답 JSON에 tagline, tags 키가 존재한다"
    - "PUT /novels/{id} 요청에 tagline, tags를 보내면 DB에 저장된다"
    - "novels 테이블에 tagline VARCHAR(255) NULL 컬럼이 존재한다"
    - "novels 테이블에 tags JSONB NOT NULL DEFAULT '[]' 컬럼이 존재한다"
    - "작품 설정 페이지 로드 시 tagline/tags 입력 필드에 저장된 값이 표시된다"
    - "변경사항 저장 클릭 시 tagline, tags가 API에 전송되고 저장된다"
    - "기존 title/genre/description 저장 기능이 그대로 동작한다 (회귀 없음)"
  artifacts:
    - path: "apps/api/alembic/versions/0007_novel_tagline_tags.py"
      provides: "tagline/tags 컬럼 추가 마이그레이션"
      contains: "op.add_column"
    - path: "apps/api/src/domains/novel/models/novel_models.py"
      provides: "Novel ORM 모델 — tagline/tags 컬럼"
      contains: "tagline"
    - path: "apps/api/src/domains/novel/schemas/novel_schemas.py"
      provides: "NovelCreate/Update/Response 스키마 확장"
      contains: "tagline"
    - path: "apps/api/src/domains/novel/router/novel_router.py"
      provides: "NovelResponse 직접 생성 시 tagline/tags 포함"
      contains: "tagline=novel.tagline"
    - path: "apps/api/src/domains/novel/service/novel_service.py"
      provides: "list_novels NovelResponse에 tagline/tags 포함"
      contains: "tagline=novel.tagline"
    - path: "apps/web/src/features/novel/types/novel.ts"
      provides: "Novel/NovelUpdateInput 인터페이스 확장"
      contains: "tagline"
    - path: "apps/web/src/features/novel/components/novel-settings-page.tsx"
      provides: "SectionBasic/draft/handleSave 통합"
      contains: "draft.tagline"
  key_links:
    - from: "apps/api/src/domains/novel/router/novel_router.py"
      to: "apps/api/src/domains/novel/schemas/novel_schemas.py"
      via: "NovelResponse(tagline=novel.tagline, tags=novel.tags)"
      pattern: "tagline=novel\\.tagline"
    - from: "apps/web/src/features/novel/components/novel-settings-page.tsx"
      to: "apps/web/src/features/novel/lib/novel-api.ts"
      via: "updateMutation.mutate({ data: { tagline, tags } })"
      pattern: "draft\\.tagline"
---

<objective>
Novel 모델에 tagline/tags 필드를 추가하고 작품 설정 페이지에서 저장까지 완전히 연결한다.

Purpose: 작가가 작품의 한 줄 소개(tagline)와 태그(tags)를 입력하여 저장할 수 있어야 한다. 현재 이 두 필드는 DB 컬럼도 없고, 프론트엔드 상태도 isolated된 로컬 useState로만 존재하여 API와 단절되어 있다.
Output: Alembic 마이그레이션 1개, 백엔드 4개 파일 수정, 프론트엔드 2개 파일 수정. SDK는 체크포인트에서 재생성.
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-작품-기본정보-api-완성/05-CONTEXT.md
@.planning/phases/05-작품-기본정보-api-완성/05-RESEARCH.md
@.planning/phases/05-작품-기본정보-api-완성/05-PATTERNS.md

<interfaces>
<!-- apps/api/src/domains/novel/models/novel_models.py — Novel 클래스 현재 컬럼 -->
<!--
class Novel(Base):
    __tablename__ = "novels"
    id: Mapped[uuid.UUID]
    user_id: Mapped[uuid.UUID]
    title: Mapped[str] = mapped_column(String(255))
    genre: Mapped[str | None] = mapped_column(String(100), nullable=True)
    description: Mapped[str | None] = mapped_column(Text, nullable=True)
    cover_image_url: Mapped[str | None] = mapped_column(String(500), nullable=True)
    created_at, updated_at ...
    chapters: relationship(...)

imports: from sqlalchemy.dialects.postgresql import JSONB  ← 이미 존재
-->

<!-- apps/api/src/domains/novel/schemas/novel_schemas.py — 현재 스키마 구조 -->
<!--
class NovelCreate(BaseModel):
    title, genre, description, cover_image_url (모두 optional=None)

class NovelUpdate(BaseModel):
    title, genre, description, cover_image_url (모두 Optional None)

class NovelResponse(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id, user_id, title, genre, description, cover_image_url,
    created_at, updated_at, chapter_count: int = 0
-->

<!-- apps/api/src/domains/novel/router/novel_router.py — NovelResponse 직접 생성 위치 -->
<!--
create_novel (line 94-104): NovelResponse(id=, user_id=, title=, genre=, description=, cover_image_url=, created_at=, updated_at=, chapter_count=0)
get_novel    (line 131-141): 동일 패턴
update_novel (line 155-165): 동일 패턴
→ 3곳 모두 tagline=novel.tagline, tags=novel.tags 추가 필요
-->

<!-- apps/api/src/domains/novel/service/novel_service.py — list_novels -->
<!--
list_novels (line 33-43): NovelResponse(id=, user_id=, title=, genre=, description=, cover_image_url=, created_at=, updated_at=, chapter_count=count)
→ 이 1곳에도 tagline=novel.tagline, tags=novel.tags 추가 필요
-->

<!-- apps/web/src/features/novel/types/novel.ts — 현재 인터페이스 -->
<!--
export interface Novel {
  id, user_id, title, genre, description, cover_image_url, created_at, updated_at, chapter_count
}
export interface NovelUpdateInput {
  title?, genre?, description?, cover_image_url?
}
-->

<!-- apps/web/src/features/novel/components/novel-settings-page.tsx — 핵심 위치 -->
<!--
SectionBasic (line 200-235):
  props: { title, genre, description, onChange }
  내부 상태(제거 대상): const [tags, setTags] = useState([...])  line 204
                         const [tagline, setTagline] = useState('...')  line 205
  WsInput tagline (line 213): onChange={setTagline}  → onChange({ tagline: v }) 로 교체
  WsTags  (line 231): onChange={(v) => setTags(v.slice(0, 10))}  → onChange({ tags: v.slice(0, 10) }) 로 교체

draft 상태 (line 569):
  const [draft, setDraft] = useState({ title: novel.title, genre: novel.genre ?? 'fantasy', description: novel.description ?? '' })

handleSave (line 577-581):
  updateMutation.mutate({ id: novel.id, data: { title, genre, description } }, ...)

SectionBasic 호출 (line 673):
  <SectionBasic title={draft.title} genre={draft.genre} description={draft.description} onChange={handleChange} />
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 백엔드 — 마이그레이션 생성 + 모델/스키마/라우터/서비스 수정</name>
  <files>
    apps/api/alembic/versions/0007_novel_tagline_tags.py,
    apps/api/src/domains/novel/models/novel_models.py,
    apps/api/src/domains/novel/schemas/novel_schemas.py,
    apps/api/src/domains/novel/router/novel_router.py,
    apps/api/src/domains/novel/service/novel_service.py
  </files>
  <read_first>
    - apps/api/alembic/versions/0006_story_beats.py — 마이그레이션 헤더/revision 패턴
    - apps/api/src/domains/novel/models/novel_models.py — Novel 클래스 기존 컬럼 위치 + JSONB import
    - apps/api/src/domains/novel/schemas/novel_schemas.py — NovelCreate/Update/Response 현재 필드
    - apps/api/src/domains/novel/router/novel_router.py — NovelResponse(...) 직접 생성 3곳 (line 94, 131, 155)
    - apps/api/src/domains/novel/service/novel_service.py — list_novels의 NovelResponse(...) 1곳 (line 33)
  </read_first>
  <behavior>
    - 마이그레이션 실행 후 novels 테이블에 tagline 컬럼(VARCHAR 255, NULL 허용)이 존재한다
    - 마이그레이션 실행 후 novels 테이블에 tags 컬럼(JSONB, NOT NULL, DEFAULT '[]'::jsonb)이 존재한다
    - GET /novels/{id} 응답에 tagline(null 또는 string), tags(string[]) 키가 포함된다
    - PUT /novels/{id} { tagline: "foo", tags: ["a"] } 전송 시 DB에 저장된다
    - PUT /novels/{id} { title: "bar" }만 전송 시 기존 tagline/tags가 변경되지 않는다 (exclude_unset=True)
    - GET /novels (목록) 응답의 각 아이템에도 tagline, tags가 포함된다
  </behavior>
  <action>
    1. 마이그레이션 파일 apps/api/alembic/versions/0007_novel_tagline_tags.py 생성 (per D-03):
       - docstring: "Add tagline and tags columns to novels table."
       - revision = "0007_novel_tagline_tags", down_revision = "0006_story_beats"
       - imports: from __future__ import annotations; import sqlalchemy as sa; from alembic import op; from sqlalchemy.dialects import postgresql
       - upgrade(): op.add_column("novels", sa.Column("tagline", sa.String(255), nullable=True)) 후
         op.add_column("novels", sa.Column("tags", postgresql.JSONB(astext_type=sa.Text()), server_default=sa.text("'[]'::jsonb"), nullable=False))
       - downgrade(): op.drop_column("novels", "tags"); op.drop_column("novels", "tagline")
       - 주의: server_default는 반드시 sa.text("'[]'::jsonb") — 문자열 단독 사용 금지

    2. apps/api/src/domains/novel/models/novel_models.py 수정 (per D-01, D-02):
       - Novel 클래스의 cover_image_url 아래에 두 컬럼 추가
       - tagline: Mapped[str | None] = mapped_column(String(255), nullable=True)
       - tags: Mapped[list[str]] = mapped_column(JSONB, server_default=sa.text("'[]'::jsonb"), nullable=False)
       - JSONB는 이미 import 되어 있음(line 10); String은 line 9에 있음
       - sa 사용을 위해 import sqlalchemy as sa 추가 필요 여부 확인 — 현재 파일에 없으면 추가
       - 주의: server_default=sa.text("'[]'::jsonb") 형식 — 문자열 "'\[\]'" 금지

    3. apps/api/src/domains/novel/schemas/novel_schemas.py 수정 (per D-04, D-05, D-06):
       - NovelCreate: cover_image_url 아래에 tagline: str | None = None, tags: list[str] = []
       - NovelUpdate: cover_image_url 아래에 tagline: str | None = None, tags: list[str] | None = None
       - NovelResponse: cover_image_url 아래에 tagline: str | None = None, tags: list[str] = []

    4. apps/api/src/domains/novel/router/novel_router.py 수정 (per NOVEL-04, Pitfall 1):
       - create_novel (line ~94), get_novel (line ~131), update_novel (line ~155) 각 NovelResponse(...) 호출에
         tagline=novel.tagline, tags=novel.tags 두 키워드 인수 추가
       - 3곳 모두 동일하게 적용 — 1곳이라도 누락 시 해당 엔드포인트 응답에서 필드 누락

    5. apps/api/src/domains/novel/service/novel_service.py 수정 (per Pitfall 1):
       - list_novels의 NovelResponse(...) 호출 (line ~33-43)에 tagline=novel.tagline, tags=novel.tags 추가
       - update_novel의 repo.update(**data.model_dump(exclude_unset=True)) 패턴은 수정 불필요
  </action>
  <verify>
    <automated>ls /Users/gyuha/workspace/storywriter-studio/apps/api/alembic/versions/0007_novel_tagline_tags.py && cd /Users/gyuha/workspace/storywriter-studio/apps/api && uv run ruff check src/domains/novel/ --select E,W,F,I,N,UP,B,C4,SIM,ANN,S,T20,PT,RUF && uv run mypy src/domains/novel/</automated>
  </verify>
  <done>
    - 0007_novel_tagline_tags.py 파일이 존재한다
    - ruff check와 mypy가 novel 도메인에서 에러 없이 통과한다
    - 0007_novel_tagline_tags.py가 올바른 revision/down_revision으로 존재한다
    - Novel 모델에 tagline/tags 컬럼이 추가되었다
    - NovelCreate, NovelUpdate, NovelResponse 각각에 tagline/tags 필드가 추가되었다
    - novel_router.py의 NovelResponse 직접 생성 3곳 모두에 tagline=novel.tagline, tags=novel.tags가 포함되었다
    - novel_service.py의 list_novels NovelResponse에 tagline=novel.tagline, tags=novel.tags가 포함되었다
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: SDK 재생성 체크포인트 — 백엔드 서버 실행 후 generate:api 실행</name>
  <what-built>
    Task 1에서 백엔드 스키마 변경 완료. SDK 재생성은 FastAPI 서버가 실행 중일 때만 가능 (openapi.json 갱신 필요).
  </what-built>
  <how-to-verify>
    1. PostgreSQL + Redis 실행 확인: cd apps/api && docker compose up -d
    2. 마이그레이션 적용: cd apps/api && uv run alembic upgrade head
       - 성공 시 "Running upgrade ... -> 0007_novel_tagline_tags" 출력
    3. FastAPI 서버 실행: cd apps/api && uv run uvicorn src.main:app --reload
       - 서버가 http://127.0.0.1:8000 에서 실행 중인 상태 유지
    4. 새 터미널에서 SDK 재생성: cd apps/web && pnpm generate:api
       - apps/web/src/generated/ 파일들이 재생성됨
    5. apps/web/src/generated/types.gen.ts 를 열어서 NovelResponse 타입에 tagline?, tags? 필드가 있는지 확인
  </how-to-verify>
  <resume-signal>SDK 재생성 완료 후 "done" 입력</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 프론트엔드 — Novel 타입 수정 + SectionBasic 상태 리프팅 + draft/handleSave 연결</name>
  <files>
    apps/web/src/features/novel/types/novel.ts,
    apps/web/src/features/novel/components/novel-settings-page.tsx
  </files>
  <read_first>
    - apps/web/src/features/novel/types/novel.ts — 현재 Novel, NovelUpdateInput 인터페이스
    - apps/web/src/features/novel/components/novel-settings-page.tsx — SectionBasic(200-235), draft(569), handleChange(572), handleSave(577), SectionBasic 호출(673)
    - apps/web/src/generated/types.gen.ts — SDK 재생성 후 NovelResponse, NovelUpdate 타입 확인
  </read_first>
  <behavior>
    - novel.tagline이 null이면 tagline 입력 필드가 빈 문자열로 초기화된다
    - novel.tags가 없으면(undefined/null) tags 입력 필드가 빈 배열로 초기화된다
    - tagline 입력 후 변경사항 저장 클릭 시 PUT /novels/{id} 요청 body에 tagline 값이 포함된다
    - tags 변경 후 변경사항 저장 클릭 시 PUT /novels/{id} 요청 body에 tags 배열이 포함된다
    - title/genre/description 저장은 기존과 동일하게 동작한다 (회귀 없음)
    - pnpm typecheck가 타입 에러 없이 통과한다
  </behavior>
  <action>
    1. apps/web/src/features/novel/types/novel.ts 수정 (per D-07, Pitfall 3):
       - Novel 인터페이스의 cover_image_url 아래에 두 필드 추가:
         tagline: string | null;
         tags: string[];
       - NovelUpdateInput 인터페이스의 cover_image_url 아래에 두 필드 추가:
         tagline?: string;
         tags?: string[];

    2. apps/web/src/features/novel/components/novel-settings-page.tsx 수정 (per D-07, D-08, D-09, Pitfall 4):

       2a. SectionBasic props 타입 확장 (line 200-202 수정):
           - title, genre, description 외에 tagline: string; tags: string[] 추가
           - onChange 타입에 tagline?: string; tags?: string[] 추가

       2b. SectionBasic 내부 useState 제거 (per Pitfall 4):
           - line 204: const [tags, setTags] = useState([...]) 제거
           - line 205: const [tagline, setTagline] = useState('...') 제거

       2c. SectionBasic 내 WsInput(tagline) onChange 교체:
           - 기존: onChange={setTagline}
           - 교체: onChange={(v) => onChange({ tagline: v })}
           - value={tagline} → value={tagline} (props에서 온 tagline 사용, 변수명 충돌 없음)

       2d. SectionBasic 내 WsTags onChange 교체:
           - 기존: onChange={(v) => setTags(v.slice(0, 10))}
           - 교체: onChange={(v) => onChange({ tags: v.slice(0, 10) })}
           - value={tags} → value={tags} (props에서 온 tags 사용)

       2e. draft 상태 확장 (line 569, per D-07):
           - 기존: { title: novel.title, genre: novel.genre ?? 'fantasy', description: novel.description ?? '' }
           - 확장: { title: novel.title, genre: novel.genre ?? 'fantasy', description: novel.description ?? '', tagline: novel.tagline ?? '', tags: novel.tags ?? [] }

       2f. handleSave 확장 (line 577-581, per D-09):
           - data 객체에 tagline: draft.tagline, tags: draft.tags 추가
           - 최종: { title: draft.title, genre: draft.genre, description: draft.description, tagline: draft.tagline, tags: draft.tags }

       2g. SectionBasic 호출 확장 (line 673):
           - tagline={draft.tagline} tags={draft.tags} props 추가

       주의: handleChange는 Partial<typeof draft>를 사용하므로 draft 확장 시 자동으로 tagline/tags를 처리함 — 변경 불필요
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>
    - pnpm typecheck가 에러 없이 통과한다
    - pnpm build가 에러 없이 완료된다
    - Novel 인터페이스에 tagline: string | null, tags: string[] 필드가 있다
    - NovelUpdateInput 인터페이스에 tagline?: string, tags?: string[] 필드가 있다
    - SectionBasic props에 tagline, tags가 포함된다
    - SectionBasic 내부의 tags/tagline useState가 제거되었다
    - draft 상태에 tagline, tags 초기값이 포함된다
    - handleSave가 tagline, tags를 updateMutation에 전달한다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PUT /novels/{id} | 사용자 입력 tagline/tags가 API 경계를 넘는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | NovelUpdate.tagline | mitigate | String(255) DB 컬럼 제약 + Pydantic str 타입 검증으로 길이 초과 시 422 반환 |
| T-05-02 | Tampering | NovelUpdate.tags | mitigate | list[str] Pydantic 타입으로 비배열 입력 거부; 프론트엔드에서 v.slice(0, 10)으로 10개 제한 |
| T-05-03 | Tampering | JSONB 컬럼 직접 SQL 주입 | accept | SQLAlchemy ORM parameterized query가 JSONB 값을 자동 이스케이프 — 별도 처리 불필요 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 설치 없음 — 패키지 감사 해당 없음 |
</threat_model>

<verification>
## 전체 Phase 검증

1. 백엔드 정적 분석:
   cd apps/api && uv run ruff check src/domains/novel/ && uv run mypy src/domains/novel/

2. 마이그레이션 적용 확인:
   cd apps/api && uv run alembic upgrade head
   -- 성공 출력: Running upgrade 0006_story_beats -> 0007_novel_tagline_tags

3. DB 스키마 확인:
   novels 테이블에 tagline (character varying 255, nullable), tags (jsonb, not null, default '[]'::jsonb) 컬럼 존재

4. API 응답 확인 (서버 실행 후):
   curl -s -X GET http://localhost:8000/novels/{id} -H "Authorization: Bearer TOKEN" | grep -E '"tagline"|"tags"'
   -- 두 키가 모두 응답에 존재해야 함

5. 프론트엔드 타입 체크:
   cd apps/web && pnpm typecheck
   -- 에러 0개

6. 프론트엔드 빌드:
   cd apps/web && pnpm build
   -- 빌드 에러 없음

7. 수동 E2E:
   - 작품 설정 페이지 접속 → 한 줄 소개/태그 수정 → 변경사항 저장 → 페이지 새로고침 → 값 유지 확인
</verification>

<success_criteria>
1. Novel API 응답(GET/PUT/POST /novels, GET /novels/{id})에 tagline, tags 필드가 포함된다
2. 작품 설정 기본 정보 섹션에서 한 줄 소개와 태그를 입력하고 저장하면 페이지 새로고침 후에도 값이 유지된다
3. 기존 title/genre/description 저장 기능이 정상 작동한다 (회귀 없음)
4. pnpm typecheck 에러 0개, ruff + mypy novel 도메인 에러 0개
5. pnpm build 에러 0개
</success_criteria>

<output>
완료 후 `.planning/phases/05-작품-기본정보-api-완성/05-01-SUMMARY.md` 파일 생성
</output>
