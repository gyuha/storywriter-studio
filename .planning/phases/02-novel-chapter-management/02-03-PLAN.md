---
phase: "02"
plan: "02-03"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/web/package.json
  - apps/web/src/features/novel/lib/chapter-api.ts
  - apps/web/src/features/novel/hooks/use-chapter-queries.ts
  - apps/web/src/features/novel/hooks/use-chapter-mutations.ts
  - apps/web/src/features/novel/hooks/use-chapter-autosave.ts
  - apps/web/src/features/novel/components/chapter-editor.tsx
  - apps/web/src/features/novel/components/save-status-bar.tsx
  - apps/web/src/routes/_authenticated/novels/$novelId/chapters/$chapterId/edit.tsx
autonomous: true
requirements:
  - CHAP-02
  - CHAP-03
  - CHAP-04

must_haves:
  truths:
    - "챕터 에디터 페이지에 TipTap 에디터가 렌더링되고 본문을 입력할 수 있다"
    - "굵게(Bold), 이탤릭(Italic), 제목(Heading) 서식이 동작한다"
    - "입력 후 3초가 지나면 자동으로 PATCH 요청이 발생한다"
    - "에디터 하단에 '저장됨' / '저장 중...' / '저장 실패' 상태가 표시된다"
    - "에디터 하단에 현재 글자 수가 실시간으로 표시된다"
    - "5,000자 이상 입력 시 글자 수 카운터가 초록색으로 바뀐다"
  artifacts:
    - path: "apps/web/src/features/novel/components/chapter-editor.tsx"
      provides: "TipTap 에디터 컴포넌트"
      contains: "useEditor, immediatelyRender: false, CharacterCount, StarterKit"
    - path: "apps/web/src/features/novel/hooks/use-chapter-autosave.ts"
      provides: "3초 debounce 자동저장 훅"
      contains: "useDebounce, useMutation, SaveStatus"
    - path: "apps/web/src/routes/_authenticated/novels/$novelId/chapters/$chapterId/edit.tsx"
      provides: "챕터 에디터 라우트"
      contains: "createFileRoute"
  key_links:
    - from: "apps/web/src/features/novel/hooks/use-chapter-autosave.ts"
      to: "chapter-api.ts apiUpdateChapter"
      via: "useMutation + useDebounce 3000ms"
      pattern: "useDebounce.*3000"
    - from: "apps/web/src/features/novel/components/chapter-editor.tsx"
      to: "use-chapter-autosave.ts"
      via: "onUpdate 콜백 → content state → autosave"
      pattern: "useChapterAutosave"

user_setup: []
---

<objective>
TipTap 에디터 기반 챕터 집필 환경을 구축한다. @tiptap/react, @tiptap/starter-kit, @tiptap/extension-character-count, use-debounce 패키지를 설치하고, ChapterEditor 컴포넌트와 useChapterAutosave 훅을 작성한다. 챕터 에디터 라우트(`/novels/:novelId/chapters/:chapterId/edit`)에서 TipTap 에디터가 동작하고, 3초 debounce 자동저장과 글자 수 표시가 모두 동작한다.

D-18: 지원 확장 — StarterKit + CharacterCount + Link (링크 지원 CHAP-02 충족).
D-28: 챕터 content는 TipTap `getJSON()` 결과(JSON 객체)로 주고받는다. 에디터는 `getHTML()` 대신 `getJSON()`을 사용하고, API는 `Record<string, unknown>` 타입으로 JSONB에 저장한다.

Purpose: 사용자가 실제로 소설을 집필할 수 있는 핵심 기능.
Output: ChapterEditor 컴포넌트, useChapterAutosave 훅, 에디터 라우트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-novel-chapter-management/02-CONTEXT.md
@.planning/phases/02-novel-chapter-management/02-RESEARCH.md
@.planning/phases/02-novel-chapter-management/02-01-SUMMARY.md
@.planning/phases/02-novel-chapter-management/02-02-SUMMARY.md

<interfaces>
<!-- Wave 2 진입 시 이미 존재해야 하는 계약 -->

apps/web/src/features/novel/types/novel.ts (02-02에서 생성):
```typescript
export interface Chapter {
  id: string;
  novel_id: string;
  title: string;
  content: Record<string, unknown> | null;  // D-28: JSONB (getJSON() 결과)
  order_key: number;
  status: ChapterStatus;
  created_at: string;
  updated_at: string;
}

export type ChapterStatus = 'draft' | 'reviewing' | 'done';
```

TipTap 필수 설정 (RESEARCH.md §1.3) — D-28 적용:
```typescript
const editor = useEditor({
  extensions: [StarterKit, CharacterCount],
  content,
  immediatelyRender: false,  // 필수 — 없으면 hydration 경고
  onUpdate: ({ editor }) => { onUpdate(editor.getJSON()); },  // getHTML() 금지, getJSON() 사용
});
```

use-debounce named import (RESEARCH.md §위험2):
```typescript
import { useDebounce } from 'use-debounce';  // 반드시 중괄호, default import 금지
```

글자 수 읽기 (RESEARCH.md §1.4):
```typescript
editor.storage.characterCount.characters()  // 전체 글자 수
```

content 초기화 타이밍 패턴 (RESEARCH.md §위험6):
```typescript
useEffect(() => {
  if (editor && chapterData?.content) {
    // content가 JSON 객체이므로 그대로 전달 (문자열 변환 금지)
    editor.commands.setContent(chapterData.content);
  }
}, [editor, chapterData?.content]);
```

chapter-api.ts가 제공해야 하는 타입 및 함수:
```typescript
export interface ChapterUpdateInput {
  title?: string;
  content?: Record<string, unknown> | null;  // D-28: JSONB
  status?: ChapterStatus;
}

export async function apiUpdateChapter(
  novelId: string,
  chapterId: string,
  data: ChapterUpdateInput
): Promise<Chapter>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 패키지 설치 + 챕터 API 클라이언트 + 훅</name>
  <files>
    apps/web/package.json,
    apps/web/src/features/novel/lib/chapter-api.ts,
    apps/web/src/features/novel/hooks/use-chapter-queries.ts,
    apps/web/src/features/novel/hooks/use-chapter-mutations.ts,
    apps/web/src/features/novel/hooks/use-chapter-autosave.ts
  </files>
  <action>
    1. 패키지 설치 (모두 검증된 패키지 — RESEARCH.md §6):
       cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm add @tiptap/react @tiptap/pm @tiptap/starter-kit @tiptap/extension-character-count @tiptap/extension-link @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities use-debounce

       (dnd-kit은 02-04에서 사용하지만 동일 wave에서 한 번에 설치)

    2. lib/chapter-api.ts 작성 — novel-api.ts와 동일한 스타일. D-28: content는 Record<string, unknown> | null 타입.
       - BASE = '/api/v1'
       - ChapterUpdateInput 인터페이스: { title?: string; content?: Record<string, unknown> | null; status?: ChapterStatus }
         (string 타입 사용 금지 — JSONB 저장이므로 JSON 객체)
       - apiGetChapters(novelId): GET /novels/:novelId/chapters → Chapter[]
       - apiCreateChapter(novelId, data: { title: string; content?: Record<string, unknown> | null }): POST → Chapter
       - apiGetChapter(novelId, chapterId): GET → Chapter
       - apiUpdateChapter(novelId, chapterId, data: ChapterUpdateInput): PUT /novels/:novelId/chapters/:chapterId → Chapter
       - apiReorderChapter(novelId, chapterId, order_key: number): PATCH /novels/:novelId/chapters/:chapterId/reorder → Chapter
       - apiDeleteChapter(novelId, chapterId): DELETE → void

    3. hooks/use-chapter-queries.ts:
       - useChapters(novelId: string): useQuery(['chapters', novelId], () => apiGetChapters(novelId), { enabled: !!novelId })
       - useChapter(novelId: string, chapterId: string): useQuery(['chapters', novelId, chapterId], () => apiGetChapter(novelId, chapterId), { enabled: !!novelId && !!chapterId })

    4. hooks/use-chapter-mutations.ts:
       - useCreateChapterMutation(): onSuccess → invalidate ['chapters', novelId], navigate to edit page
       - useUpdateChapterMutation(): onSuccess → invalidate ['chapters', novelId, chapterId]
       - useDeleteChapterMutation(): onSuccess → navigate to /novels/:novelId, invalidate ['chapters', novelId]
       - useReorderChapterMutation(): onSuccess → invalidate ['chapters', novelId]

    5. hooks/use-chapter-autosave.ts:
       - SaveStatus 타입: 'idle' | 'saving' | 'saved' | 'error'
       - useChapterAutosave(novelId: string, chapterId: string, content: Record<string, unknown> | null): { saveStatus: SaveStatus }
         (content 파라미터 타입이 string이 아닌 JSON 객체 — D-28)
       - useDebounce(content, 3000) — named import 필수 ({ useDebounce })
       - useEffect: debouncedContent 변경 시 mutation.mutate({ content: debouncedContent }) 호출
       - onMutate → 'saving', onSuccess → 'saved', onError → 'error'
       - content가 null이거나 빈 객체(Object.keys가 0개)일 때는 저장 건너뜀
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <done>
    - package.json에 @tiptap/react, @dnd-kit/core, use-debounce 추가됨
    - chapter-api.ts의 ChapterUpdateInput.content가 Record<string, unknown> | null 타입 (string 아님)
    - chapter-api.ts, use-chapter-queries.ts, use-chapter-mutations.ts, use-chapter-autosave.ts 존재
    - use-chapter-autosave.ts의 content 파라미터가 Record<string, unknown> | null 타입
    - pnpm typecheck에서 해당 파일 관련 오류 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: ChapterEditor 컴포넌트 + 에디터 라우트</name>
  <files>
    apps/web/src/features/novel/components/chapter-editor.tsx,
    apps/web/src/features/novel/components/save-status-bar.tsx,
    apps/web/src/routes/_authenticated/novels/$novelId/chapters/$chapterId/edit.tsx
  </files>
  <action>
    1. features/novel/components/save-status-bar.tsx:
       - Props: saveStatus('idle'|'saving'|'saved'|'error'), charCount(number), charTarget(number)
       - 글자 수 색상: charCount >= charTarget → 'text-green-500', 아니면 'text-gray-400'
       - 저장 상태 텍스트: idle→"" , saving→"저장 중...", saved→"저장됨", error→"저장 실패"
       - 에러 색상: error → 'text-red-500'
       - 레이아웃: flex justify-between, 좌측 글자 수, 우측 저장 상태

    2. features/novel/components/chapter-editor.tsx:
       - Props: novelId(string), chapterId(string), initialContent(Record<string, unknown> | null)
         (initialContent 타입이 string이 아닌 JSON 객체 — D-28)
       - useEditor 설정:
         extensions: [StarterKit, CharacterCount, Link.configure({ openOnClick: false })]
         immediatelyRender: false  (필수)
         content: null  (초기값 — useEffect로 교체)
         onUpdate: ({ editor }) → setContent(editor.getJSON())
         (getHTML() 사용 금지 — D-28에 따라 반드시 getJSON() 사용)
       - useState<Record<string, unknown> | null>로 content 관리 (로컬 편집 상태 — D-15)
         (string 타입 사용 금지)
       - useEffect: editor와 initialContent가 모두 있을 때 editor.commands.setContent(initialContent) 실행
         initialContent는 이미 JSON 객체이므로 파싱 없이 직접 전달
       - useChapterAutosave(novelId, chapterId, content) → saveStatus
       - 툴바: Bold, Italic, Heading (H1, H2) 토글 버튼 + Link 버튼
         editor.chain().focus().toggleBold().run()
         editor.isActive('bold') ? 활성화 스타일 : 기본 스타일
         Link 버튼: Ctrl+K 단축키 지원. 클릭 시 window.prompt()로 URL 입력 후 editor.chain().focus().setLink({ href: url }).run() 호출.
         URL이 비어 있거나 취소 시 editor.chain().focus().unsetLink().run() 호출. editor.isActive('link') 여부로 활성화 스타일 적용.
       - EditorContent 렌더링
       - SaveStatusBar 렌더링 (에디터 아래)
       - charCount: editor?.storage.characterCount.characters() ?? 0

    3. routes/_authenticated/novels/$novelId/chapters/$chapterId/edit.tsx:
       - createFileRoute('/_authenticated/novels/$novelId/chapters/$chapterId/edit')
       - Route.useParams()로 novelId, chapterId 추출
       - useChapter(novelId, chapterId)로 챕터 데이터 로드
       - isLoading → 전체 화면 로딩 스피너
       - isError → "챕터를 불러올 수 없습니다" 에러 메시지
       - 레이아웃: 전체 화면 (h-screen), 좌측 사이드바(챕터 목록 — 02-04 플랜에서 완성) + 우측 에디터 영역
       - 사이드바 자리: placeholder div ("챕터 목록 로드 중...")로 준비해 두기 (width: w-64)
       - 에디터 영역: flex-1, ChapterEditor 컴포넌트 렌더링
         initialContent={chapter.content}  // Record<string, unknown> | null 타입 그대로 전달
       - 페이지 상단: 챕터 제목 표시

    TipTap 에디터 CSS: prose 클래스를 EditorContent 감싸는 div에 적용하거나, 기본 Tailwind Typography 없이 직접 스타일링.
    apps/web/src/routes/_authenticated/novels/$novelId/chapters/$chapterId/ 디렉토리가 없으면 먼저 생성.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep -v "^$" | tail -30</automated>
  </verify>
  <done>
    - chapter-editor.tsx 존재, immediatelyRender: false 포함
    - chapter-editor.tsx에서 getJSON() 사용 (getHTML() 사용 없음) — D-28
    - chapter-editor.tsx에서 Link 확장 포함, 링크 툴바 버튼(Ctrl+K) 동작 — CHAP-02
    - chapter-editor.tsx의 content state 타입이 Record<string, unknown> | null
    - save-status-bar.tsx 존재, 5000자 색상 분기 로직 포함
    - routes/.../edit.tsx 존재
    - pnpm typecheck 통과
    - pnpm build에서 컴파일 오류 없음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | 자동저장 PUT 요청, 챕터 content(JSON 객체)를 서버로 전송 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | DoS | 자동저장 debounce | mitigate | 3초 debounce로 과도한 PUT 요청 방지. null/빈 객체 건너뜀 처리 |
| T-02-08 | Tampering | content 서식 (JSON) | accept | TipTap getJSON() 출력은 서버에서 JSONB로 저장됨. 렌더링 시 TipTap이 자체 파싱하므로 XSS 위험 낮음. 외부 렌더링 시 sanitize 필요 (향후 Phase) |
| T-02-SC | Tampering | npm 패키지 설치 | mitigate | RESEARCH.md §6 패키지 정당성 감사 완료 (모두 승인). postinstall 스크립트 없음 확인됨 |
</threat_model>

<verification>
```bash
# 타입 체크
cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck

# TipTap 패키지 설치 확인
grep -v '^#' /Users/gyuha/workspace/storywriter-studio/apps/web/package.json | grep tiptap

# use-debounce named import 패턴 확인
grep -n "useDebounce" /Users/gyuha/workspace/storywriter-studio/apps/web/src/features/novel/hooks/use-chapter-autosave.ts

# immediatelyRender: false 확인
grep -n "immediatelyRender" /Users/gyuha/workspace/storywriter-studio/apps/web/src/features/novel/components/chapter-editor.tsx

# getJSON() 사용 확인 (getHTML() 없어야 함) — D-28
grep -n "getJSON\|getHTML" /Users/gyuha/workspace/storywriter-studio/apps/web/src/features/novel/components/chapter-editor.tsx

# content 타입 확인 — Record<string, unknown> 사용
grep -n "Record<string, unknown>" /Users/gyuha/workspace/storywriter-studio/apps/web/src/features/novel/lib/chapter-api.ts
```
</verification>

<success_criteria>
- @tiptap/react, @tiptap/starter-kit, @tiptap/extension-character-count, @tiptap/extension-link, use-debounce 패키지 설치됨
- chapter-api.ts: ChapterUpdateInput.content가 Record<string, unknown> | null (string 아님) — D-28
- chapter-editor.tsx: immediatelyRender: false, CharacterCount 확장, onUpdate에서 getJSON() 사용 (getHTML() 금지) — D-28
- chapter-editor.tsx: content state 타입이 Record<string, unknown> | null, initialContent props도 동일 타입
- chapter-editor.tsx: setContent(initialContent) 호출 시 JSON 객체 직접 전달 (문자열 변환 없음)
- use-chapter-autosave.ts: content 파라미터가 Record<string, unknown> | null, useDebounce(content, 3000) named import
- chapter-editor.tsx: Link.configure({ openOnClick: false }) 확장 포함, 링크 삽입 툴바 버튼(Ctrl+K) 동작 — CHAP-02
- save-status-bar.tsx: 5000자 이상 초록색 분기, 저장 상태 텍스트 3종
- /novels/:novelId/chapters/:chapterId/edit 라우트 존재
- pnpm typecheck 통과
</success_criteria>

<output>
완료 후 `.planning/phases/02-novel-chapter-management/02-03-SUMMARY.md` 생성
</output>
