---
phase: "02"
plan: "02-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/alembic/versions/0003_novel_domain.py
  - apps/api/alembic/env.py
  - apps/api/src/domains/novel/__init__.py
  - apps/api/src/domains/novel/models/__init__.py
  - apps/api/src/domains/novel/models/novel_models.py
  - apps/api/src/domains/novel/schemas/__init__.py
  - apps/api/src/domains/novel/schemas/novel_schemas.py
  - apps/api/src/domains/novel/repository/__init__.py
  - apps/api/src/domains/novel/repository/novel_repository.py
  - apps/api/src/domains/novel/repository/chapter_repository.py
  - apps/api/src/domains/novel/service/__init__.py
  - apps/api/src/domains/novel/service/novel_service.py
  - apps/api/src/domains/novel/service/chapter_service.py
  - apps/api/src/domains/novel/router/__init__.py
  - apps/api/src/domains/novel/router/novel_router.py
  - apps/api/src/main.py
  - apps/api/tests/unit/novel/__init__.py
  - apps/api/tests/unit/novel/test_novel_service.py
  - apps/api/tests/unit/novel/test_chapter_service.py
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
  - CHAP-01
  - CHAP-05
  - CHAP-06

must_haves:
  truths:
    - "GET /api/v1/novels 호출 시 현재 사용자의 소설 목록만 반환한다"
    - "POST /api/v1/novels 로 소설을 생성하면 201과 생성된 소설 데이터를 반환한다"
    - "다른 사용자의 소설에 접근하면 403을 반환한다"
    - "소설 삭제 시 연관 챕터도 cascade 삭제된다"
    - "챕터 생성 시 order_key가 자동으로 할당된다 (마지막 챕터 + 1000.0)"
    - "챕터 reorder PATCH 요청으로 order_key를 갱신할 수 있다"
    - "챕터 status는 draft/reviewing/done 세 값만 허용된다"
    - "NovelResponse의 chapter_count가 실제 챕터 수를 반환한다 (항상 0이 아님)"
  artifacts:
    - path: "apps/api/src/domains/novel/models/novel_models.py"
      provides: "Novel, Chapter SQLAlchemy ORM 모델"
      contains: "class Novel, class Chapter, class ChapterStatus"
    - path: "apps/api/src/domains/novel/schemas/novel_schemas.py"
      provides: "Pydantic 요청/응답 스키마"
      contains: "NovelCreate, NovelResponse, ChapterCreate, ChapterResponse, ChapterReorderRequest"
    - path: "apps/api/src/domains/novel/router/novel_router.py"
      provides: "FastAPI APIRouter"
      exports: ["router"]
    - path: "apps/api/alembic/versions/0003_novel_domain.py"
      provides: "novels + chapters 테이블 마이그레이션"
      contains: "chapter_status_enum, postgresql.JSONB"
  key_links:
    - from: "apps/api/src/domains/novel/router/novel_router.py"
      to: "domains.auth.security.get_current_user"
      via: "Depends(get_current_user)"
      pattern: "get_current_user"
    - from: "apps/api/src/domains/novel/service/novel_service.py"
      to: "novel.user_id"
      via: "ForbiddenError if novel.user_id != user_id"
      pattern: "ForbiddenError"
    - from: "apps/api/alembic/env.py"
      to: "domains.novel.models"
      via: "try import"
      pattern: "_novel_models"
    - from: "apps/api/src/domains/novel/repository/novel_repository.py"
      to: "chapters 테이블"
      via: "scalar_subquery COUNT"
      pattern: "chapter_count_subq"
---

<objective>
`domains/novel/` 전체 백엔드 도메인을 구축한다. DB 마이그레이션(novels + chapters 테이블)부터 SQLAlchemy 모델, Pydantic 스키마, Repository, Service, Router까지 auth 도메인 패턴을 그대로 따라 구현하고, main.py에 라우터를 등록한다. 소설/챕터 CRUD 엔드포인트가 모두 동작하고, 소유권 검증이 서비스 레이어에서 이루어진다.

Purpose: Wave 2 프론트엔드 플랜들이 실제 API를 호출할 수 있도록 백엔드 기반을 완성한다.
Output: 동작하는 REST API (`/api/v1/novels`, `/api/v1/novels/{id}/chapters`), 단위 테스트, Alembic 마이그레이션.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-novel-chapter-management/02-CONTEXT.md
@.planning/phases/02-novel-chapter-management/02-RESEARCH.md

<interfaces>
<!-- 기존 auth 도메인 패턴 — executor가 그대로 따라야 하는 계약 -->

apps/api/src/core/exceptions.py 에서:
```python
class AppError(Exception):
    def __init__(self, message: str, status_code: int = 400) -> None: ...

class NotFoundError(AppError):  # 404
    def __init__(self, resource: str = "Resource") -> None: ...

class ForbiddenError(AppError):  # 403
    def __init__(self, message: str = "Forbidden.") -> None: ...

class ConflictError(AppError):  # 409
    def __init__(self, message: str = "Conflict.") -> None: ...
```

apps/api/src/domains/auth/security.py 에서:
```python
async def get_current_user(
    token_ctx: AccessTokenContext = Depends(get_current_access_token_context),
    session: AsyncSession = Depends(get_async_session),
) -> User:
    ...  # User 객체 반환. user.id: uuid.UUID
```

apps/api/src/core/database.py 에서:
```python
async def get_async_session() -> AsyncGenerator[AsyncSession, None]: ...
Base: DeclarativeBase  # 모든 ORM 모델이 상속
```

apps/api/src/main.py _register_routers 패턴:
```python
try:
    from domains.novel.router.novel_router import router as novel_router
    application.include_router(novel_router, prefix="/api/v1")
    logger.debug("router_registered", prefix="/api/v1/novels")
except ImportError:
    logger.debug("novel_router_not_found")
```

alembic/env.py 모델 import 패턴:
```python
try:
    from domains.novel import models as _novel_models  # noqa: F401
except ImportError:
    logger.debug("novel models not found — skipping")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB 마이그레이션 + Novel 도메인 모델/스키마/레포지토리</name>
  <files>
    apps/api/alembic/versions/0003_novel_domain.py,
    apps/api/alembic/env.py,
    apps/api/src/domains/novel/__init__.py,
    apps/api/src/domains/novel/models/__init__.py,
    apps/api/src/domains/novel/models/novel_models.py,
    apps/api/src/domains/novel/schemas/__init__.py,
    apps/api/src/domains/novel/schemas/novel_schemas.py,
    apps/api/src/domains/novel/repository/__init__.py,
    apps/api/src/domains/novel/repository/novel_repository.py,
    apps/api/src/domains/novel/repository/chapter_repository.py
  </files>
  <behavior>
    - Novel 모델: id(UUID PK, server_default=uuid_generate_v4()), user_id(FK→users CASCADE), title(String 255), genre(String 100 nullable), description(Text nullable), cover_image_url(String 500 nullable), created_at/updated_at(DateTime timezone=True, server_default=now())
    - Chapter 모델: id(UUID PK), novel_id(FK→novels CASCADE), title(String 255), content(JSONB nullable — D-28: TipTap getJSON() 결과 저장), order_key(Float nullable=False), status(SAEnum ChapterStatus nullable=False default=DRAFT), created_at/updated_at
    - ChapterStatus: Python enum.Enum("draft", "reviewing", "done") — SAEnum name="chapter_status_enum"
    - 마이그레이션 revision="0003_novel_domain", down_revision="0002_seed_admin_role_and_permission"
    - upgrade(): CREATE TYPE chapter_status_enum → create novels 테이블 → create chapters 테이블 (content 컬럼은 postgresql.JSONB 타입) → 인덱스(novels.user_id, chapters.novel_id, chapters.order_key)
    - downgrade(): drop chapters → drop novels → DROP TYPE chapter_status_enum (순서 중요)
    - alembic/env.py: chat import except 블록 종료 직후(현재 L139), target_metadata 할당 라인(L141) 이전에 novel models import 블록 삽입 (try/except 패턴)
    - NovelRepository:
        get_by_id(id) → Novel | None
        list_by_user(user_id, offset=0, limit=20) → tuple[list[tuple[Novel, int]], int]
          (chapter_count를 subquery로 함께 조회 — 아래 구현 패턴 참고)
        create(user_id, **fields) → Novel
        update(novel, **fields) → Novel
        delete(novel) → None
    - list_by_user 구현 패턴:
        chapter_count_subq = (
            select(func.count(Chapter.id))
            .where(Chapter.novel_id == Novel.id)
            .correlate(Novel)
            .scalar_subquery()
        )
        stmt = (
            select(Novel, chapter_count_subq.label("chapter_count"))
            .where(Novel.user_id == user_id)
            .order_by(Novel.updated_at.desc())
            .offset(offset)
            .limit(limit)
        )
        rows = (await session.execute(stmt)).all()
        # 반환: (Novel 객체, chapter_count 정수) 튜플 목록
        count_stmt = select(func.count()).select_from(Novel).where(Novel.user_id == user_id)
        total = (await session.execute(count_stmt)).scalar_one()
        return rows, total
    - ChapterRepository: get_by_id(novel_id, chapter_id) → Chapter | None, list_by_novel(novel_id) → list[Chapter] (order_key ASC), create(novel_id, **fields) → Chapter, update(chapter, **fields) → Chapter, delete(chapter) → None, get_last_chapter(novel_id) → Chapter | None
    - Pydantic 스키마:
        NovelCreate(title, genre?, description?, cover_image_url?)
        NovelUpdate(title?, genre?, description?, cover_image_url?)
        NovelResponse(id, user_id, title, genre, description, cover_image_url, created_at, updated_at, chapter_count: int = 0), from_attributes=True
        ChapterCreate(title, content?: dict | None = None)
        ChapterUpdate(title?, content?: dict | None = None, status?)
          — content 타입은 dict | None (D-28: JSONB)
        ChapterReorderRequest(order_key: float)
        ChapterResponse(id, novel_id, title, content: dict | None = None, order_key, status, created_at, updated_at), from_attributes=True
          — content 타입은 dict | None (D-28: JSONB)
        NovelListResponse(items: list[NovelResponse], total: int)
  </behavior>
  <action>
    1. apps/api/src/domains/novel/ 디렉토리 트리 전체 생성 (모든 __init__.py 포함).
    2. novel_models.py 작성 — Novel, Chapter, ChapterStatus. mapped_column() + Mapped[] 타입 힌트 사용 (SQLAlchemy 2.0 스타일). Novel.updated_at에는 onupdate=datetime.now(UTC) 추가. Chapter.content 컬럼은 JSONB 타입(D-28): from sqlalchemy.dialects.postgresql import JSONB 임포트 후 mapped_column(JSONB, nullable=True) 사용.
    3. novel_schemas.py 작성 — behavior에 명시된 Pydantic 모델. model_config = ConfigDict(from_attributes=True). ChapterCreate.content, ChapterUpdate.content, ChapterResponse.content 모두 dict | None = None 타입.
    4. novel_repository.py 작성 — list_by_user()는 behavior의 chapter_count_subq 패턴을 정확히 구현한다. select(Novel, chapter_count_subq.label("chapter_count")) 쿼리로 Novel과 chapter_count를 함께 조회하여 (Novel, int) 튜플 목록으로 반환한다.
    5. chapter_repository.py 작성 — AsyncSession 주입, select/func.count/order_by 패턴.
    6. alembic/versions/0003_novel_domain.py 작성: chapters 테이블의 content 컬럼은 반드시 postgresql.JSONB 타입 사용. from sqlalchemy.dialects import postgresql 임포트 후 sa.Column("content", postgresql.JSONB, nullable=True) 형태로 작성. order_key는 sa.Float().
    7. alembic/env.py 수정: chat import except 블록 종료 직후(현재 L139), target_metadata 할당 라인(L141) 이전에 novel models import 블록 삽입. 기존 try/except 패턴과 동일하게 작성.
    8. docker compose가 올라와 있다면 마이그레이션 적용: cd apps/api && uv run alembic upgrade head
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/api && uv run python -c "from domains.novel.models.novel_models import Novel, Chapter, ChapterStatus; from domains.novel.schemas.novel_schemas import NovelCreate, NovelResponse, ChapterCreate, ChapterResponse; print('imports OK')"</automated>
  </verify>
  <done>
    - domains/novel/ 디렉토리 구조 완성 (models, schemas, repository, service, router 각 __init__.py 포함)
    - alembic 0003 마이그레이션 파일 존재, chapters.content 컬럼이 postgresql.JSONB 타입
    - alembic/env.py에 novel models import 추가됨 (chat import except 블록 직후)
    - Python import 오류 없음
    - NovelCreate, NovelResponse, ChapterCreate, ChapterResponse 스키마 임포트 성공
    - ChapterResponse.content, ChapterUpdate.content, ChapterCreate.content가 dict | None 타입
    - NovelRepository.list_by_user()가 chapter_count_subq 패턴으로 실제 챕터 수 반환
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Novel/Chapter 서비스 + 라우터 + main.py 등록 + 단위 테스트</name>
  <files>
    apps/api/src/domains/novel/service/__init__.py,
    apps/api/src/domains/novel/service/novel_service.py,
    apps/api/src/domains/novel/service/chapter_service.py,
    apps/api/src/domains/novel/router/__init__.py,
    apps/api/src/domains/novel/router/novel_router.py,
    apps/api/src/main.py,
    apps/api/tests/unit/novel/__init__.py,
    apps/api/tests/unit/novel/test_novel_service.py,
    apps/api/tests/unit/novel/test_chapter_service.py
  </files>
  <behavior>
    - NovelService:
        create_novel(user_id, data: NovelCreate) → Novel
        get_novel(id, user_id) → Novel (없으면 NotFoundError, 소유권 다르면 ForbiddenError)
        list_novels(user_id, offset, limit) → tuple[list[NovelResponse], int]
          NovelRepository.list_by_user()가 반환하는 (Novel, chapter_count) 튜플 목록을 순회하여,
          각 항목을 NovelResponse(id=novel.id, ..., chapter_count=count)로 변환한다.
          chapter_count=0으로 하드코딩하지 말 것.
        update_novel(id, user_id, data: NovelUpdate) → Novel
        delete_novel(id, user_id) → None
    - ChapterService: create_chapter(novel_id, user_id, data: ChapterCreate) → Chapter (order_key 자동 할당: get_last_chapter().order_key + 1000.0, 없으면 1000.0), get_chapter(novel_id, chapter_id, user_id) → Chapter, list_chapters(novel_id, user_id) → list[Chapter], update_chapter(novel_id, chapter_id, user_id, data: ChapterUpdate) → Chapter, reorder_chapter(novel_id, chapter_id, user_id, order_key: float) → Chapter, delete_chapter(novel_id, chapter_id, user_id) → None
    - 소유권 검증: 챕터 접근 시 먼저 novel.user_id == user_id 확인 (novel 자체가 없으면 NotFoundError, 소유권 다르면 ForbiddenError)
    - Router: prefix="/novels", tags=["novels"]. _get_novel_service, _get_chapter_service dependency 함수로 주입. _app_error_to_http() 헬퍼로 AppError → HTTPException 변환.
    - 소설 엔드포인트: POST /novels (201), GET /novels (200, query: offset=0&limit=20), GET /novels/{id} (200), PUT /novels/{id} (200), DELETE /novels/{id} (204)
    - 챕터 엔드포인트: POST /novels/{novel_id}/chapters (201), GET /novels/{novel_id}/chapters (200), GET /novels/{novel_id}/chapters/{id} (200), PUT /novels/{novel_id}/chapters/{id} (200), PATCH /novels/{novel_id}/chapters/{id}/reorder (200), DELETE /novels/{novel_id}/chapters/{id} (204)
    - 단위 테스트 (pytest, Mock 기반): test_novel_service.py — test_create_novel, test_get_novel_not_found, test_get_novel_forbidden, test_list_novels_filters_by_user(chapter_count가 실제 값으로 채워지는지 검증), test_delete_novel
    - test_chapter_service.py — test_create_chapter_assigns_order_key, test_create_chapter_increments_order_key, test_reorder_chapter, test_delete_chapter
  </behavior>
  <action>
    1. novel_service.py 작성 — NovelRepository를 생성자에서 받음. list_novels()는 repo.list_by_user()가 반환하는 (Novel, int) 튜플을 순회하여 NovelResponse 객체로 변환한다. chapter_count=0으로 하드코딩하지 않고 반드시 subquery 결과값(int)을 사용한다.
    2. chapter_service.py 작성 — NovelRepository + ChapterRepository 모두 받음. create_chapter 시 get_last_chapter로 order_key 계산.
    3. novel_router.py 작성 — auth_router.py 구조 그대로: import 블록, router = APIRouter(prefix="/novels", tags=["novels"]), _get_novel_service/chapter_service dependency 함수, _app_error_to_http 헬퍼. 모든 엔드포인트에 current_user: User = Depends(get_current_user).
    4. main.py _register_routers 함수에 novel router 등록 블록 추가 (Chat domain 등록 이전에 삽입).
    5. tests/unit/novel/ 생성. test_novel_service.py와 test_chapter_service.py 작성 — unittest.mock.AsyncMock으로 repo를 mock, 서비스만 단위 테스트. test_list_novels_filters_by_user 테스트에서는 mock repo가 [(Novel_mock, 3)] 형태의 튜플을 반환하도록 설정하고, NovelResponse.chapter_count == 3임을 검증한다.
    6. 테스트 실행 확인.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/api && uv run pytest tests/unit/novel/ -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - novel_service.py, chapter_service.py 존재, AppError 기반 소유권 검증 포함
    - novel_service.list_novels()가 NovelResponse.chapter_count를 subquery 결과로 채움 (하드코딩 0 아님)
    - novel_router.py 존재, prefix="/novels"
    - main.py에 novel router 등록됨
    - pytest tests/unit/novel/ 전체 통과 (PASSED)
    - test_list_novels_filters_by_user에서 chapter_count가 실제 값(mock 반환값)으로 채워짐을 검증
    - import 오류 없음: uv run python -c "from domains.novel.router.novel_router import router; print(router.prefix)"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | HTTP 요청의 novel_id, chapter_id는 신뢰 불가 — 항상 소유권 검증 필요 |
| Service → DB | AsyncSession 쿼리 결과는 신뢰 — ORM 레벨에서 타입 보장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | novel_service.get_novel | mitigate | novel.user_id != user_id → ForbiddenError (서비스 레이어에서 검증) |
| T-02-02 | Information Disclosure | GET /novels | mitigate | list_by_user에 user_id 필터 적용 — 타인 소설 목록 접근 불가 |
| T-02-03 | Tampering | chapter_service 소유권 | mitigate | 챕터 접근 전 반드시 novel 소유권 검증 후 chapter 조회 |
| T-02-04 | DoS | content JSONB 무제한 | accept | MVP 단계 — 실제 트래픽 발생 전 제한 추가. content 크기 제한은 Phase 5에서 검토 |
| T-02-SC | Tampering | pip/uv 패키지 | accept | 백엔드에 신규 패키지 없음 (기존 SQLAlchemy/FastAPI/Alembic 재사용) |
</threat_model>

<verification>
Docker가 실행 중인 상태에서:

```bash
# 마이그레이션 적용 확인
cd /Users/gyuha/workspace/storywriter-studio/apps/api && uv run alembic current

# 단위 테스트 전체 통과
uv run pytest tests/unit/novel/ -v

# 서버 기동 시 라우터 등록 확인 (임포트 오류 없음)
uv run python -c "from main import create_app; app = create_app(); routes = [r.path for r in app.routes]; print([r for r in routes if 'novel' in r])"

# JSONB 타입 확인
grep -n "JSONB" /Users/gyuha/workspace/storywriter-studio/apps/api/alembic/versions/0003_novel_domain.py

# chapter_count subquery 패턴 확인
grep -n "chapter_count_subq" /Users/gyuha/workspace/storywriter-studio/apps/api/src/domains/novel/repository/novel_repository.py
```
</verification>

<success_criteria>
- alembic/versions/0003_novel_domain.py 존재, novels + chapters 테이블 생성 SQL 포함
- chapters.content 컬럼이 postgresql.JSONB 타입 (TEXT 아님) — D-28 준수
- alembic/env.py에 novel models import 추가 (chat import except 블록 직후, target_metadata 할당 이전)
- domains/novel/ 전체 구조 (models, schemas, repository, service, router) 완성
- ChapterResponse.content, ChapterUpdate.content, ChapterCreate.content 모두 dict | None 타입
- NovelRepository.list_by_user()가 scalar_subquery로 chapter_count를 실제 DB에서 집계
- NovelService.list_novels()가 chapter_count를 0으로 하드코딩하지 않고 repo 결과에서 채움
- pytest tests/unit/novel/ PASSED (소설 CRUD + 챕터 CRUD + 소유권 검증 + chapter_count 검증 케이스 포함)
- main.py에 novel_router 등록 (prefix="/api/v1/novels")
- FastAPI `/docs`에서 /novels 엔드포인트 그룹 확인 가능
</success_criteria>

<output>
완료 후 `.planning/phases/02-novel-chapter-management/02-01-SUMMARY.md` 생성
</output>
