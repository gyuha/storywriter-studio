---
phase: 01-auth-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/features/auth/types/auth.ts
  - apps/web/src/features/auth/store/auth.store.ts
  - apps/web/src/features/auth/lib/auth-api.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "auth.ts가 백엔드 UserResponse와 1:1 대응하는 AuthUser 타입을 export한다"
    - "auth-api.ts가 실제 FastAPI 엔드포인트를 호출하는 apiLogin, apiSignup, apiGetMe, apiLogout를 export한다"
    - "auth.store.ts의 useAuthStore가 확장된 AuthUser 타입을 받아들인다"
    - "mock-auth-api.ts가 삭제된다"
  artifacts:
    - path: "apps/web/src/features/auth/types/auth.ts"
      provides: "AuthUser(id,email,display_name,is_verified,is_active,created_at), TokenResponse, LoginInput, SignupInput"
      exports: ["AuthUser", "TokenResponse", "LoginInput", "SignupInput"]
    - path: "apps/web/src/features/auth/lib/auth-api.ts"
      provides: "실제 fetch 기반 API 함수"
      exports: ["apiLogin", "apiSignup", "apiGetMe", "apiLogout"]
  key_links:
    - from: "apps/web/src/features/auth/lib/auth-api.ts"
      to: "/api/v1/auth/login, /api/v1/auth/me, /api/v1/auth/signup, /api/v1/auth/logout"
      via: "fetch"
      pattern: "fetch.*api/v1/auth"
---

<objective>
프론트엔드 auth 타입과 API 레이어를 실제 백엔드와 일치하도록 교체한다. mock-auth-api.ts를 삭제하고 실제 fetch 기반 함수들을 작성한다.

Purpose: AUTH-01/02의 기반. 이후 Plan 03의 useLoginMutation, useSignupMutation, useInitAuth가 이 레이어를 사용한다.
Output: 확장된 auth.ts 타입, 실제 API 호출 함수 auth-api.ts, auth.store.ts 타입 호환성 유지
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-integration/01-CONTEXT.md
@.planning/phases/01-auth-integration/01-PATTERNS.md

<interfaces>
<!-- 백엔드 UserResponse (auth_schemas.py) — AuthUser 타입의 소스 of truth -->
```python
class UserResponse(BaseModel):
    id: uuid.UUID          # → string (UUID)
    email: EmailStr        # → string
    display_name: str | None  # → string | null
    is_verified: bool      # → boolean
    is_active: bool        # → boolean
    created_at: datetime   # → string (ISO)
```

<!-- 백엔드 TokenResponse -->
```python
class TokenResponse(BaseModel):
    access_token: str
    refresh_token: str
    token_type: str   # 'bearer'
    expires_in: int
```

<!-- 백엔드 SignupRequest -->
```python
class SignupRequest(BaseModel):
    email: EmailStr
    password: str
    display_name: str | None = None  # 프론트엔드 name 필드 → display_name으로 매핑 필요
```

<!-- 현재 mock-auth-api.ts — 삭제 대상 -->
현재 파일: apps/web/src/features/auth/lib/mock-auth-api.ts
삭제 후 이 파일을 import하는 곳: apps/web/src/features/auth/hooks/use-auth-mutation.ts (Plan 03에서 수정)

<!-- 현재 auth.store.ts — 타입만 교체, 구조 유지 -->
```typescript
export const useAuthStore = create<AuthState>((set) => ({
  isAuthenticated: false,
  user: null,
  setUser: (user) => set({ isAuthenticated: true, user }),
  clearUser: () => set({ isAuthenticated: false, user: null }),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: auth.ts 타입 확장 및 auth.store.ts 호환성 업데이트</name>
  <files>
    apps/web/src/features/auth/types/auth.ts
    apps/web/src/features/auth/store/auth.store.ts
  </files>
  <read_first>
    - apps/web/src/features/auth/types/auth.ts (현재 파일 전체 — 현재 AuthUser, AuthResponse, LoginInput, SignupInput 확인)
    - apps/web/src/features/auth/store/auth.store.ts (현재 파일 전체 — AuthState 인터페이스 확인)
    - apps/web/src/features/auth/schema/ (스키마 파일 존재 여부 확인 — zod 스키마와 타입 연동 여부)
  </read_first>
  <action>
**auth.ts 교체** (per PATTERNS.md types/auth.ts 섹션):

기존 `AuthUser { name: string; email: string }` → 아래로 교체:

```typescript
export interface AuthUser {
  id: string;
  email: string;
  display_name: string | null;
  is_verified: boolean;
  is_active: boolean;
  created_at: string;
}

export interface TokenResponse {
  access_token: string;
  refresh_token: string;
  token_type: 'bearer';
  expires_in: number;
}

// LoginInput 유지 (기존과 동일)
export interface LoginInput {
  email: string;
  password: string;
}

// SignupInput 유지 (name 필드 그대로 — api 호출 시 display_name으로 매핑, per Pitfall 5)
export interface SignupInput {
  name: string;
  email: string;
  password: string;
  confirmPassword: string;
}
```

기존 `AuthResponse` 타입은 삭제 (mock 전용, 실제 API와 무관).

**auth.store.ts 수정**: `AuthUser` import는 동일. store 구조(`setUser`, `clearUser`, `isAuthenticated`, `user`) 변경 없음. `AuthUser` 타입이 확장되었으므로 TypeScript가 새 타입을 자동으로 사용함. store 파일 자체에는 로직 변경 없이 import 경로만 확인하여 유지.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep -E "auth.ts|auth.store.ts|error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/features/auth/types/auth.ts에 `AuthUser` 인터페이스가 `id: string`, `email: string`, `display_name: string | null`, `is_verified: boolean`, `is_active: boolean`, `created_at: string` 필드를 포함한다
    - `TokenResponse` 인터페이스가 `access_token`, `refresh_token`, `token_type`, `expires_in` 필드를 포함한다
    - 기존 `AuthResponse` 타입이 제거된다
    - pnpm typecheck에서 auth.ts, auth.store.ts 관련 타입 오류가 없다
  </acceptance_criteria>
  <done>AuthUser가 백엔드 UserResponse와 1:1 대응. TokenResponse 추가. typecheck 통과.</done>
</task>

<task type="auto">
  <name>Task 2: auth-api.ts 생성 및 mock-auth-api.ts 삭제</name>
  <files>
    apps/web/src/features/auth/lib/auth-api.ts
    apps/web/src/features/auth/lib/mock-auth-api.ts
  </files>
  <read_first>
    - apps/web/src/features/auth/lib/mock-auth-api.ts (현재 파일 전체 — 삭제 전 함수 시그니처 파악)
    - apps/web/src/features/auth/hooks/use-auth-mutation.ts (mock-auth-api import 위치 확인 — Plan 03에서 수정할 것이므로 지금은 파악만)
  </read_first>
  <action>
**auth-api.ts 신규 생성** (per PATTERNS.md auth-api.ts 섹션, D-01 localStorage 저장):

```typescript
// Named exports only — CLAUDE.md 규칙
import type { AuthUser, TokenResponse } from '../types/auth';

const BASE = '/api/v1';

export async function apiLogin(email: string, password: string): Promise<TokenResponse> {
  const res = await fetch(`${BASE}/auth/login`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email, password }),
  });
  if (!res.ok) {
    const err = await res.json().catch(() => ({}));
    throw new Error((err as { detail?: string }).detail ?? '로그인에 실패했습니다');
  }
  return res.json() as Promise<TokenResponse>;
}

export async function apiSignup(email: string, password: string, displayName: string): Promise<void> {
  const res = await fetch(`${BASE}/auth/signup`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email, password, display_name: displayName }),
  });
  if (!res.ok) {
    const err = await res.json().catch(() => ({}));
    throw new Error((err as { detail?: string }).detail ?? '회원가입에 실패했습니다');
  }
}

export async function apiGetMe(token: string): Promise<AuthUser> {
  const res = await fetch(`${BASE}/auth/me`, {
    headers: { Authorization: `Bearer ${token}` },
  });
  if (!res.ok) throw new Error('401');
  return res.json() as Promise<AuthUser>;
}

export async function apiLogout(accessToken: string, refreshToken: string): Promise<void> {
  await fetch(`${BASE}/auth/logout`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${accessToken}`,
    },
    body: JSON.stringify({ refresh_token: refreshToken }),
  });
}
```

**mock-auth-api.ts 삭제**: 파일 제거. (use-auth-mutation.ts가 현재 이 파일을 import하고 있으므로 삭제 후 TypeScript 에러가 발생하지만, 이는 Plan 03에서 수정함 — 현재 단계에서는 의도된 상태)
  </action>
  <verify>
    <automated>test -f /Users/gyuha/workspace/storywriter-studio/apps/web/src/features/auth/lib/auth-api.ts && echo "auth-api.ts exists" && test ! -f /Users/gyuha/workspace/storywriter-studio/apps/web/src/features/auth/lib/mock-auth-api.ts && echo "mock-auth-api.ts deleted"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/features/auth/lib/auth-api.ts 파일이 존재하고 `apiLogin`, `apiSignup`, `apiGetMe`, `apiLogout` 4개 함수를 named export한다
    - `apiSignup` 함수가 세 번째 파라미터를 `display_name: displayName`으로 JSON body에 포함하여 POST /api/v1/auth/signup를 호출한다 (per Pitfall 5 — name → display_name 매핑)
    - `apiGetMe`가 `Authorization: Bearer {token}` 헤더와 함께 GET /api/v1/auth/me를 호출한다
    - apps/web/src/features/auth/lib/mock-auth-api.ts 파일이 존재하지 않는다
    - `grep -r "mock-auth-api" apps/web/src/features/auth/lib/` 결과가 없다
  </acceptance_criteria>
  <done>auth-api.ts 생성 완료. mock-auth-api.ts 삭제 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→FastAPI | fetch 호출로 JWT 토큰 교환 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | localStorage access_token 저장 | accept | D-01/D-03 결정으로 localStorage 사용 확정 — httpOnly cookie 미사용. MVP 수용. |
| T-01-05 | Spoofing | apiLogin fetch 응답 파싱 | mitigate | `res.ok` 체크 후 `err.detail` 추출. 네트워크 오류는 catch로 처리하여 임의 문자열이 throw되지 않도록 `.catch(() => ({}))` 방어 패턴 사용 |
| T-01-SC | Tampering | (해당 없음 — 신규 패키지 설치 없음) | accept | 이 Plan은 신규 패키지를 설치하지 않음 |
</threat_model>

<verification>
```bash
# 타입 검사
cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep -v "mock-auth-api\|use-auth-mutation" | grep "error TS"
# 기대값: auth.ts, auth.store.ts, auth-api.ts 관련 오류 없음 (use-auth-mutation.ts 오류는 Plan 03 전까지 허용)

# 파일 존재 확인
ls apps/web/src/features/auth/lib/
# 기대값: auth-api.ts 존재, mock-auth-api.ts 미존재

# export 확인
grep "^export" apps/web/src/features/auth/lib/auth-api.ts
# 기대값: apiLogin, apiSignup, apiGetMe, apiLogout 4개
```
</verification>

<success_criteria>
- auth.ts의 AuthUser가 백엔드 UserResponse와 필드 일치 (id, email, display_name, is_verified, is_active, created_at)
- TokenResponse 타입 존재
- auth-api.ts에 4개 API 함수 (apiLogin, apiSignup, apiGetMe, apiLogout) 존재
- mock-auth-api.ts 삭제됨
- auth.ts, auth.store.ts, auth-api.ts에서 typecheck 오류 없음
</success_criteria>

<output>
작업 완료 후 `.planning/phases/01-auth-integration/01-02-SUMMARY.md` 생성
</output>
