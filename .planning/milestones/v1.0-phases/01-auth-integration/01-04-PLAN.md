---
phase: 01-auth-integration
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-03
files_modified:
  - apps/web/src/features/admin/types/admin.ts
  - apps/web/src/features/admin/hooks/use-admin-users.ts
  - apps/web/src/features/admin/components/admin-users-page.tsx
  - apps/web/src/routes/_authenticated/admin/users.tsx
autonomous: true
requirements:
  - AUTH-04
must_haves:
  truths:
    - "관리자가 /admin/users 페이지에서 전체 사용자 목록을 페이지네이션으로 조회할 수 있다"
    - "관리자가 각 사용자 행의 활성화/비활성화 버튼을 클릭하면 즉시 반영되어 목록이 갱신된다"
    - "비인증 사용자가 /admin/users에 접근하면 /auth/login으로 리다이렉트된다"
    - "일반 사용자가 /admin/users에 접근하면 백엔드 403 응답을 받아 에러 토스트가 표시된다"
  artifacts:
    - path: "apps/web/src/features/admin/types/admin.ts"
      provides: "AdminUser, PaginatedUsersResponse 타입"
      exports: ["AdminUser", "PaginatedUsersResponse"]
    - path: "apps/web/src/features/admin/hooks/use-admin-users.ts"
      provides: "useAdminUsers(useQuery), useActivateUser(useMutation), useDeactivateUser(useMutation)"
      exports: ["useAdminUsers", "useActivateUser", "useDeactivateUser"]
    - path: "apps/web/src/features/admin/components/admin-users-page.tsx"
      provides: "사용자 목록 테이블 + 활성화/비활성화 버튼 UI"
      exports: ["AdminUsersPage"]
    - path: "apps/web/src/routes/_authenticated/admin/users.tsx"
      provides: "TanStack Router 라우트 파일"
  key_links:
    - from: "apps/web/src/routes/_authenticated/admin/users.tsx"
      to: "apps/web/src/features/admin/components/admin-users-page.tsx"
      via: "component: AdminUsersPage"
      pattern: "AdminUsersPage"
    - from: "apps/web/src/features/admin/hooks/use-admin-users.ts"
      to: "/api/v1/admin/users"
      via: "fetch with Authorization header"
      pattern: "fetch.*admin/users"
---

<objective>
관리자 사용자 관리 UI를 구현한다. sample/users를 참조하여 production 코드를 features/admin/ 아래에 새로 작성하고, _authenticated/admin/users.tsx 라우트로 연결한다.

Purpose: AUTH-04 프론트엔드 구현. Plan 01의 백엔드 API와 연결하여 관리자 사용자 관리 기능을 완성.
Output: admin 타입, admin hooks, AdminUsersPage 컴포넌트, admin 라우트 파일
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-integration/01-CONTEXT.md
@.planning/phases/01-auth-integration/01-PATTERNS.md
@.planning/phases/01-auth-integration/01-01-SUMMARY.md
@.planning/phases/01-auth-integration/01-03-SUMMARY.md

<interfaces>
<!-- Plan 01 백엔드 API 스펙 -->
```
GET  /api/v1/admin/users?page={n}&size={n}
     → PaginatedUsersResponse { items: AdminUser[], total: int, page: int, size: int }

POST /api/v1/admin/users/{user_id}/activate   → 204 No Content
POST /api/v1/admin/users/{user_id}/deactivate → 204 No Content

모든 엔드포인트: Authorization: Bearer {access_token} 헤더 필수
권한 없으면 403 Forbidden
```

<!-- Plan 01의 AdminUserResponse → 프론트엔드 AdminUser 타입 대응 -->
```typescript
// 백엔드 AdminUserResponse 필드:
// id: UUID, email: str, display_name: str|None, is_verified: bool, is_active: bool, created_at: datetime
export interface AdminUser {
  id: string;
  email: string;
  display_name: string | null;
  is_verified: boolean;
  is_active: boolean;
  created_at: string;
}
```

<!-- sample/users 참조 구조 (per D-16) -->
apps/web/src/sample/users/ — UI 참조용. production 코드는 features/admin/ 아래 신규 작성.
주요 컴포넌트: DataTable, DataTablePagination — @tanstack/react-table 기반, 이미 프로젝트에 존재.

<!-- Plan 03의 인증 패턴 -->
```typescript
// localStorage Bearer 토큰 헤더 패턴
function getAuthHeader(): HeadersInit {
  const token = localStorage.getItem('access_token');
  return token ? { Authorization: `Bearer ${token}` } : {};
}
```

<!-- TanStack Router 파일 기반 라우트 패턴 -->
```typescript
// _authenticated 하위 라우트 (per D-15)
// 파일: apps/web/src/routes/_authenticated/admin/users.tsx
export const Route = createFileRoute('/_authenticated/admin/users')({
  component: AdminUsersPage,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin 타입 + use-admin-users.ts 훅 구현</name>
  <files>
    apps/web/src/features/admin/types/admin.ts
    apps/web/src/features/admin/hooks/use-admin-users.ts
  </files>
  <read_first>
    - apps/web/src/features/auth/types/auth.ts (타입 파일 구조 패턴 — named export 확인)
    - apps/web/src/features/auth/hooks/use-auth-mutation.ts (useMutation + useQueryClient.invalidateQueries 패턴)
    - apps/web/src/sample/users/ (디렉토리 전체 파일 목록 확인 — 어떤 타입/훅이 있는지 참조)
    - apps/web/src/features/admin/ (디렉토리가 이미 존재하는지 확인, 존재 시 기존 파일 확인)
  </read_first>
  <action>
**features/admin/types/admin.ts 생성** (per PATTERNS.md admin.ts 섹션, D-16):

```typescript
export interface AdminUser {
  id: string;
  email: string;
  display_name: string | null;
  is_verified: boolean;
  is_active: boolean;
  created_at: string;
}

export interface PaginatedUsersResponse {
  items: AdminUser[];
  total: number;
  page: number;
  size: number;
}
```

**features/admin/hooks/use-admin-users.ts 생성** (per PATTERNS.md use-admin-users.ts 섹션):

```typescript
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { toast } from 'sonner';
import type { PaginatedUsersResponse } from '../types/admin';

const BASE = '/api/v1';

function getAuthHeader(): HeadersInit {
  const token = localStorage.getItem('access_token');
  return token ? { Authorization: `Bearer ${token}` } : {};
}

export function useAdminUsers(params: { page: number; size: number }) {
  return useQuery<PaginatedUsersResponse>({
    queryKey: ['admin', 'users', params],
    queryFn: async () => {
      const res = await fetch(
        `${BASE}/admin/users?page=${params.page}&size=${params.size}`,
        { headers: getAuthHeader() }
      );
      if (!res.ok) {
        const err = await res.json().catch(() => ({}));
        throw new Error((err as { detail?: string }).detail ?? '사용자 목록을 불러오지 못했습니다');
      }
      return res.json();
    },
  });
}

export function useActivateUser() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (userId: string) => {
      const res = await fetch(`${BASE}/admin/users/${userId}/activate`, {
        method: 'POST',
        headers: getAuthHeader(),
      });
      if (!res.ok) {
        const err = await res.json().catch(() => ({}));
        throw new Error((err as { detail?: string }).detail ?? '활성화에 실패했습니다');
      }
    },
    onSuccess: () => {
      toast.success('계정이 활성화되었습니다');
      void queryClient.invalidateQueries({ queryKey: ['admin', 'users'] });
    },
    onError: (error) => {
      toast.error(error instanceof Error ? error.message : '오류가 발생했습니다');
    },
  });
}

export function useDeactivateUser() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (userId: string) => {
      const res = await fetch(`${BASE}/admin/users/${userId}/deactivate`, {
        method: 'POST',
        headers: getAuthHeader(),
      });
      if (!res.ok) {
        const err = await res.json().catch(() => ({}));
        throw new Error((err as { detail?: string }).detail ?? '비활성화에 실패했습니다');
      }
    },
    onSuccess: () => {
      toast.success('계정이 비활성화되었습니다');
      void queryClient.invalidateQueries({ queryKey: ['admin', 'users'] });
    },
    onError: (error) => {
      toast.error(error instanceof Error ? error.message : '오류가 발생했습니다');
    },
  });
}
```
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep "error TS" | grep -E "admin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/features/admin/types/admin.ts에 `AdminUser`와 `PaginatedUsersResponse` 타입이 named export로 존재한다
    - apps/web/src/features/admin/hooks/use-admin-users.ts에 `useAdminUsers`, `useActivateUser`, `useDeactivateUser` 3개 훅이 named export로 존재한다
    - `useActivateUser`와 `useDeactivateUser`의 onSuccess에서 `queryClient.invalidateQueries({ queryKey: ['admin', 'users'] })`를 호출한다
    - pnpm typecheck에서 admin 관련 타입 오류 없음
  </acceptance_criteria>
  <done>admin 타입 + 3개 훅 생성 완료. invalidateQueries로 목록 자동 갱신.</done>
</task>

<task type="auto">
  <name>Task 2: AdminUsersPage 컴포넌트 + 라우트 파일 생성</name>
  <files>
    apps/web/src/features/admin/components/admin-users-page.tsx
    apps/web/src/routes/_authenticated/admin/users.tsx
  </files>
  <read_first>
    - apps/web/src/sample/users/components/users-page.tsx (레이아웃 구조 전체 — DataTable, 헤더, 페이지네이션 패턴)
    - apps/web/src/sample/users/components/data-table/ (DataTable 컴포넌트 import 경로 확인)
    - apps/web/src/routes/sample/users/index.tsx (createFileRoute 패턴 확인)
    - apps/web/src/routes/_authenticated/ (디렉토리 존재 여부 확인 — admin/users.tsx 경로 생성 필요)
    - apps/web/src/sample/users/components/columns.tsx (columns 정의 패턴 확인 — 활성화/비활성화 액션 셀 참조용)
  </read_first>
  <action>
**features/admin/components/admin-users-page.tsx 생성** (per D-16 — sample/users 참조하되 production 코드는 features/admin/에 신규 작성):

sample/users/components/users-page.tsx의 레이아웃 구조(헤더 + 섹션 + DataTable)를 따름.

- `useAdminUsers`, `useActivateUser`, `useDeactivateUser` 훅 사용
- 상태: `page` (useState, 초기값 1), `size` 20으로 고정 (offset 페이지네이션, per discretion)
- columns 배열을 컴포넌트 파일 내부 또는 별도 파일로 정의 (email, display_name, is_verified, is_active, created_at, actions)
- actions 셀: `is_active` 값에 따라 "활성화" 또는 "비활성화" 버튼 표시
  - is_active=true → "비활성화" 버튼(useDeactivateUser 호출)
  - is_active=false → "활성화" 버튼(useActivateUser 호출)
- isLoading 상태: "로딩 중..." 텍스트 또는 skeleton
- isError 상태: "데이터를 불러오지 못했습니다" 텍스트
- 페이지네이션: total/size로 총 페이지 계산, prev/next 버튼
- i18n: `useTranslation()` 훅 (sample/users/components/users-page.tsx 패턴 따름) — 키 문자열은 한국어 fallback으로 작성

**sample DataTable 재사용 여부**: sample/users/components/data-table/data-table.tsx가 production 코드에서 import 가능하면 재사용. 불가능하면 간단한 `<table>` 기반 직접 구현 (테이블 UI 단순화). 단, `@tanstack/react-table`의 `useReactTable`, `getCoreRowModel`은 직접 사용 가능.

**routes/_authenticated/admin/users.tsx 생성** (per D-15):

```typescript
import { AdminUsersPage } from '@/features/admin/components/admin-users-page';
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/_authenticated/admin/users')({
  component: AdminUsersPage,
});
```

**주의:** 디렉토리 `apps/web/src/routes/_authenticated/admin/`가 없으면 먼저 생성. `routeTree.gen.ts`는 Vite devserver 재시작 시 자동 갱신됨 — 직접 편집 금지.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep "error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/features/admin/components/admin-users-page.tsx가 존재하고 `AdminUsersPage` 컴포넌트를 named export한다
    - AdminUsersPage가 `useAdminUsers` 훅을 호출하여 서버 데이터를 사용한다
    - AdminUsersPage에 사용자 행마다 활성화/비활성화 버튼이 있고, `is_active` 상태에 따라 다른 버튼이 표시된다
    - apps/web/src/routes/_authenticated/admin/users.tsx가 존재하고 `createFileRoute('/_authenticated/admin/users')` 선언이 있다
    - pnpm typecheck 오류 없음
    - `grep "AdminUsersPage" apps/web/src/routes/_authenticated/admin/users.tsx` 결과가 존재한다
  </acceptance_criteria>
  <done>AdminUsersPage 컴포넌트 + 라우트 생성 완료. AUTH-04 프론트엔드 구현 완성.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→admin API | Bearer 토큰으로 인증된 관리자 요청 |
| 일반 사용자→/admin/users | 403 응답 — UI에서 에러 토스트로 처리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Elevation of Privilege | GET /admin/users 프론트엔드 접근 | mitigate | _authenticated.tsx 가드 (토큰 없으면 /auth/login 리다이렉트) + 백엔드 require_permission("admin:users") (403) |
| T-01-10 | Tampering | activate/deactivate 버튼 클릭 | mitigate | user_id를 URL 파라미터로만 사용, 요청 body 없음 — 인젝션 없음. 백엔드가 UUID 유효성 검사 |
| T-01-11 | Information Disclosure | 관리자 사용자 목록 노출 | accept | admin:users 권한 체크로 보호됨. MVP 범위에서 추가 마스킹 불필요 |
| T-01-SC | Tampering | (해당 없음 — 신규 패키지 설치 없음) | accept | 이 Plan은 신규 패키지를 설치하지 않음 |
</threat_model>

<verification>
```bash
# 타입 검사
cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck
# 기대값: 0 errors

# admin 관련 파일 존재 확인
ls apps/web/src/features/admin/types/admin.ts
ls apps/web/src/features/admin/hooks/use-admin-users.ts
ls apps/web/src/features/admin/components/admin-users-page.tsx
ls apps/web/src/routes/_authenticated/admin/users.tsx

# 빌드 확인
cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm build
# 기대값: 성공 (빌드 오류 없음)
```

## 통합 검증 (Phase 1 전체)
```bash
# 백엔드 기동 확인
cd /Users/gyuha/workspace/storywriter-studio/apps/api
docker compose up -d && uv run alembic upgrade head

# 프론트엔드 개발 서버
cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm dev

# 수동 검증 시나리오:
# 1. /auth/login → 실제 이메일/비밀번호로 로그인 → 메인 페이지로 이동
# 2. 브라우저 새로고침 → 로그인 상태 유지 (세션 복원)
# 3. 로그아웃 후 보호 라우트 접근 → /auth/login 리다이렉트
# 4. /auth/signup → 회원가입 → 자동 로그인
# 5. 관리자 계정으로 /admin/users → 사용자 목록 표시
```
</verification>

<success_criteria>
- pnpm typecheck 0 errors
- pnpm build 성공
- /admin/users 라우트 접근 시 TanStack Router가 /_authenticated/admin/users 라우트를 매칭
- AdminUsersPage가 렌더링되고 useAdminUsers가 GET /api/v1/admin/users를 호출
- 활성화/비활성화 버튼 클릭 시 sonner 토스트 표시 및 목록 자동 갱신
- AUTH-04 요구사항: 관리자가 사용자 목록 조회 및 계정 활성화/비활성화 가능
</success_criteria>

<output>
작업 완료 후 `.planning/phases/01-auth-integration/01-04-SUMMARY.md` 생성
</output>
