---
phase: "02"
plan: "02-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/features/novel/types/novel.ts
  - apps/web/src/features/novel/lib/novel-api.ts
  - apps/web/src/features/novel/hooks/use-novel-queries.ts
  - apps/web/src/features/novel/hooks/use-novel-mutations.ts
  - apps/web/src/features/novel/components/novel-card.tsx
  - apps/web/src/features/novel/components/novel-create-modal.tsx
  - apps/web/src/features/novel/components/novel-edit-modal.tsx
  - apps/web/src/routes/_authenticated/novels/index.tsx
  - apps/web/src/routes/_authenticated/novels/$novelId/index.tsx
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04

must_haves:
  truths:
    - "사용자가 /novels 에서 본인 소설 목록을 카드 그리드로 볼 수 있다"
    - "'새 소설 만들기' 버튼 클릭 시 모달이 열리고 제목/장르/설명을 입력해 소설을 생성할 수 있다"
    - "/novels/:novelId 에서 소설 제목, 장르, 설명 등 기본 정보가 표시된다"
    - "소설 상세 페이지에서 수정/삭제 버튼이 동작한다"
    - "소설 삭제 시 목록 페이지로 리다이렉트된다"
  artifacts:
    - path: "apps/web/src/features/novel/types/novel.ts"
      provides: "Novel, Chapter TypeScript 인터페이스"
      contains: "Novel, ChapterStatus, NovelCreateInput, NovelUpdateInput"
    - path: "apps/web/src/features/novel/lib/novel-api.ts"
      provides: "API 호출 함수"
      exports: ["apiCreateNovel", "apiGetNovels", "apiGetNovel", "apiUpdateNovel", "apiDeleteNovel"]
    - path: "apps/web/src/routes/_authenticated/novels/index.tsx"
      provides: "소설 목록 라우트 컴포넌트"
      contains: "createFileRoute"
    - path: "apps/web/src/routes/_authenticated/novels/$novelId/index.tsx"
      provides: "소설 상세 라우트 컴포넌트"
      contains: "createFileRoute"
  key_links:
    - from: "apps/web/src/routes/_authenticated/novels/index.tsx"
      to: "features/novel/hooks/use-novel-queries.ts"
      via: "useNovels() hook"
      pattern: "useNovels"
    - from: "apps/web/src/features/novel/lib/novel-api.ts"
      to: "/api/v1/novels"
      via: "fetch with Authorization header"
      pattern: "Authorization.*Bearer"
---

<objective>
소설 목록 페이지(`/novels`)와 소설 상세 페이지(`/novels/:novelId`)를 구현한다. `features/novel/` 디렉토리에 타입, API 클라이언트, TanStack Query 훅, UI 컴포넌트를 작성한다. 소설 생성은 모달 방식으로 구현한다 (기존 modal-store 패턴이 복잡하므로 자체 useState 기반 모달 사용 — D-24 discretion 적용).

Purpose: 사용자가 소설 프로젝트를 생성·조회·수정·삭제할 수 있는 첫 번째 수직 슬라이스.
Output: /novels, /novels/:novelId 라우트 동작, 소설 CRUD 완성.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-novel-chapter-management/02-CONTEXT.md
@.planning/phases/02-novel-chapter-management/02-RESEARCH.md

<interfaces>
<!-- 기존 프론트엔드 패턴 — executor가 그대로 따라야 하는 계약 -->

apps/web/src/features/auth/lib/auth-api.ts 패턴:
```typescript
const BASE = '/api/v1';

export async function apiLogin(email: string, password: string): Promise<TokenResponse> {
  const res = await fetch(`${BASE}/auth/login`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email, password }),
  });
  if (!res.ok) {
    const err = await res.json().catch(() => ({}));
    throw new Error((err as { detail?: string }).detail ?? '오류가 발생했습니다');
  }
  return res.json() as Promise<TokenResponse>;
}
```

Authorization 헤더 패턴:
```typescript
const token = localStorage.getItem('access_token') ?? '';
headers: { Authorization: `Bearer ${token}` }
```

apps/web/src/features/auth/hooks/use-auth-mutation.ts 패턴:
```typescript
import { useMutation } from '@tanstack/react-query';
import { toast } from 'sonner';

export function useLoginMutation() {
  return useMutation({
    mutationFn: async (data: LoginInput) => { ... },
    onSuccess: (user) => { ... },
    onError: (error) => {
      toast.error(error instanceof Error ? error.message : '오류가 발생했습니다');
    },
  });
}
```

TanStack Router 파일 기반 라우팅:
```typescript
// apps/web/src/routes/_authenticated/novels/index.tsx
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/_authenticated/novels/')({
  component: NovelsPage,
});

function NovelsPage() { ... }
```

apps/web/src/routes/_authenticated.tsx (레이아웃):
```typescript
export const Route = createFileRoute('/_authenticated')({
  beforeLoad: () => {
    const token = localStorage.getItem('access_token');
    if (!token) { throw redirect({ to: '/auth/login' }); }
  },
  component: () => <Outlet />,
});
```

cn() 유틸 (clsx + tailwind-merge):
```typescript
import { cn } from '@/lib/utils';  // 또는 clsx + twMerge 직접 사용
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Novel 타입 정의 + API 클라이언트 + TanStack Query 훅</name>
  <files>
    apps/web/src/features/novel/types/novel.ts,
    apps/web/src/features/novel/lib/novel-api.ts,
    apps/web/src/features/novel/hooks/use-novel-queries.ts,
    apps/web/src/features/novel/hooks/use-novel-mutations.ts
  </files>
  <action>
    1. features/novel/ 디렉토리 트리 생성: types/, lib/, hooks/, components/

    2. types/novel.ts — 다음 인터페이스 정의:
       - Novel: id(string), user_id(string), title(string), genre(string|null), description(string|null), cover_image_url(string|null), created_at(string), updated_at(string), chapter_count(number)
       - ChapterStatus: 'draft' | 'reviewing' | 'done' 타입
       - Chapter: id, novel_id, title, content(Record<string, unknown> | null), order_key(number), status(ChapterStatus), created_at, updated_at — D-28: JSONB 저장
       - NovelCreateInput: title(string), genre?(string), description?(string), cover_image_url?(string)
       - NovelUpdateInput: title?(string), genre?(string), description?(string), cover_image_url?(string)
       - NovelListResponse: items(Novel[]), total(number)

    3. lib/novel-api.ts — BASE='/api/v1' 고정. 인증 헤더는 localStorage.getItem('access_token')으로 가져옴. auth-api.ts와 동일한 에러 처리 패턴 사용:
       - apiGetNovels(offset=0, limit=20): GET /novels → NovelListResponse
       - apiCreateNovel(data: NovelCreateInput): POST /novels → Novel
       - apiGetNovel(id: string): GET /novels/:id → Novel
       - apiUpdateNovel(id: string, data: NovelUpdateInput): PUT /novels/:id → Novel
       - apiDeleteNovel(id: string): DELETE /novels/:id → void

    4. hooks/use-novel-queries.ts:
       - useNovels(offset=0, limit=20): useQuery({ queryKey: ['novels', offset, limit], queryFn: () => apiGetNovels(offset, limit) })
       - useNovel(id: string): useQuery({ queryKey: ['novels', id], queryFn: () => apiGetNovel(id), enabled: !!id })

    5. hooks/use-novel-mutations.ts:
       - useCreateNovelMutation(): useMutation, onSuccess에서 queryClient.invalidateQueries(['novels']), toast.success('소설이 생성되었습니다')
       - useUpdateNovelMutation(): useMutation, onSuccess에서 invalidateQueries(['novels', id]), toast.success('수정되었습니다')
       - useDeleteNovelMutation(): useMutation, onSuccess에서 navigate({ to: '/novels' }), queryClient.invalidateQueries(['novels']), toast.success('삭제되었습니다')

    모든 파일에서 named export만 사용 (default export 없음).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep -E "(error|Error)" | head -20</automated>
  </verify>
  <done>
    - features/novel/types/novel.ts, lib/novel-api.ts, hooks/use-novel-queries.ts, hooks/use-novel-mutations.ts 존재
    - pnpm typecheck에서 novel 관련 타입 오류 없음
    - Chapter.content가 `Record<string, unknown> | null` 타입으로 정의됨 (D-28)
    - Named export 패턴 (no default exports)
  </done>
</task>

<task type="auto">
  <name>Task 2: 소설 목록 + 상세 페이지 라우트 컴포넌트</name>
  <files>
    apps/web/src/features/novel/components/novel-card.tsx,
    apps/web/src/features/novel/components/novel-create-modal.tsx,
    apps/web/src/features/novel/components/novel-edit-modal.tsx,
    apps/web/src/routes/_authenticated/novels/index.tsx,
    apps/web/src/routes/_authenticated/novels/$novelId/index.tsx
  </files>
  <action>
    1. features/novel/components/novel-card.tsx:
       - Props: novel(Novel), onEdit(() => void), onDelete(() => void)
       - 표시: 커버 이미지 또는 회색 placeholder(cover_image_url 없을 때), 제목, 장르(없으면 "장르 없음"), 챕터 수, 최종 수정일(date-fns format 사용)
       - 카드 클릭 시 Link to="/novels/$novelId" (novelId=novel.id)
       - 수정/삭제 버튼은 카드 우측 상단에 아이콘 버튼으로 배치 (lucide-react의 Pencil, Trash2)
       - Tailwind CSS 카드 스타일 (rounded-lg, border, shadow-sm, hover:shadow-md)

    2. features/novel/components/novel-create-modal.tsx:
       - Props: open(boolean), onClose(() => void)
       - react-hook-form + zodResolver 사용
       - 필드: title(필수, 1-255자), genre(선택), description(선택, textarea)
       - 제출 시 useCreateNovelMutation 호출, 성공하면 onClose()
       - Base UI 또는 Radix UI Dialog를 사용하거나, 간단히 overlay + div 조합도 허용

    3. features/novel/components/novel-edit-modal.tsx:
       - Props: novel(Novel), open(boolean), onClose(() => void)
       - novel 초기값으로 폼 채움 (useEffect + reset)
       - useUpdateNovelMutation 호출

    4. routes/_authenticated/novels/index.tsx:
       - createFileRoute('/_authenticated/novels/')
       - useState로 createModalOpen 관리
       - useNovels() 훅으로 데이터 로드
       - isLoading → 로딩 스피너 (lucide-react Loader2)
       - 소설 카드 그리드 (grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4)
       - 빈 상태: "아직 소설이 없습니다. 첫 소설을 만들어 보세요!" 문구
       - 우측 상단 "새 소설 만들기" 버튼 (Plus 아이콘)
       - NovelCreateModal, NovelEditModal 조합 사용

    5. routes/_authenticated/novels/$novelId/index.tsx:
       - createFileRoute('/_authenticated/novels/$novelId/')
       - Route.useParams()로 novelId 추출
       - useNovel(novelId) 훅
       - 소설 기본 정보 표시 (제목, 장르, 설명, 커버 이미지)
       - 수정/삭제 버튼
       - "챕터 추가" 버튼 (클릭 시 useCreateChapterMutation — Wave 2에서 완성되나 버튼 자리만 준비)
       - 챕터 목록은 Wave 2 플랜에서 완성되므로 여기서는 "챕터 목록은 에디터에서 확인하세요" placeholder 문구

    라우트 파일 디렉토리가 없으면 먼저 생성:
    mkdir -p apps/web/src/routes/_authenticated/novels/$novelId
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep -v "^$" | tail -20</automated>
  </verify>
  <done>
    - novel-card.tsx, novel-create-modal.tsx, novel-edit-modal.tsx 존재
    - routes/_authenticated/novels/index.tsx, routes/_authenticated/novels/$novelId/index.tsx 존재
    - pnpm typecheck 오류 없음 (또는 novel 관련 오류만 없음)
    - pnpm build (또는 pnpm dev 시작)에서 컴파일 오류 없음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | access_token을 localStorage에서 읽어 Authorization 헤더로 전달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | localStorage access_token | accept | Phase 1에서 결정된 저장 방식. XSS 방어는 React 기본 이스케이핑에 의존 |
| T-02-06 | Tampering | novel 삭제 버튼 UI | mitigate | 삭제 전 window.confirm() 또는 별도 확인 모달로 실수 삭제 방지 |
| T-02-SC | Tampering | pnpm add 없음 (Wave 1 install에 포함) | accept | 이 플랜은 신규 패키지 설치 없음 |
</threat_model>

<verification>
```bash
# 타입 체크
cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck

# 빌드 확인
cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm build 2>&1 | tail -10

# 라우트 파일 존재 확인
ls /Users/gyuha/workspace/storywriter-studio/apps/web/src/routes/_authenticated/novels/
ls /Users/gyuha/workspace/storywriter-studio/apps/web/src/routes/_authenticated/novels/\$novelId/
```
</verification>

<success_criteria>
- features/novel/ 디렉토리 구조 완성 (types, lib, hooks, components)
- /novels 라우트 컴포넌트 존재, NovelCard 그리드 + 생성 모달 포함
- /novels/$novelId 라우트 컴포넌트 존재, 수정/삭제 동작
- pnpm typecheck 통과
- 신규 패키지 설치 없음 (기존 TanStack Query, react-hook-form, zod, lucide-react 사용)
</success_criteria>

<output>
완료 후 `.planning/phases/02-novel-chapter-management/02-02-SUMMARY.md` 생성
</output>
