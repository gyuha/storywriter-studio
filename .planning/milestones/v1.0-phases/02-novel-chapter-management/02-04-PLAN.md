---
phase: "02"
plan: "02-04"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - apps/web/src/features/novel/lib/order-key.ts
  - apps/web/src/features/novel/components/chapter-sortable-list.tsx
  - apps/web/src/features/novel/components/chapter-sidebar.tsx
  - apps/web/src/features/novel/components/chapter-status-badge.tsx
  - apps/web/src/routes/_authenticated/novels/$novelId/chapters/$chapterId/edit.tsx
autonomous: true
requirements:
  - CHAP-01
  - CHAP-05
  - CHAP-06

must_haves:
  truths:
    - "에디터 페이지 좌측 사이드바에 현재 소설의 챕터 목록이 표시된다"
    - "사이드바에서 챕터를 클릭하면 해당 챕터 에디터로 이동한다"
    - "챕터를 드래그앤드롭으로 순서를 바꿀 수 있고, 변경 후 서버에 order_key가 PATCH된다"
    - "사이드바에서 챕터 상태(초안/검토 중/완성)를 드롭다운으로 변경할 수 있다"
    - "사이드바 하단 '+ 챕터 추가' 버튼으로 새 챕터를 생성하고 에디터로 이동한다"
  artifacts:
    - path: "apps/web/src/features/novel/lib/order-key.ts"
      provides: "Fractional indexing 계산 함수"
      exports: ["calcOrderKey", "needsReindex"]
    - path: "apps/web/src/features/novel/components/chapter-sortable-list.tsx"
      provides: "@dnd-kit SortableList 컴포넌트"
      contains: "DndContext, SortableContext, useSortable, arrayMove"
    - path: "apps/web/src/features/novel/components/chapter-sidebar.tsx"
      provides: "챕터 목록 사이드바 (DnD + 상태 변경 + 추가)"
      contains: "ChapterSortableList, useCreateChapterMutation"
  key_links:
    - from: "apps/web/src/features/novel/components/chapter-sortable-list.tsx"
      to: "features/novel/lib/order-key.ts calcOrderKey"
      via: "onDragEnd → calcOrderKey → useReorderChapterMutation"
      pattern: "calcOrderKey"
    - from: "apps/web/src/routes/_authenticated/novels/$novelId/chapters/$chapterId/edit.tsx"
      to: "features/novel/components/chapter-sidebar.tsx"
      via: "ChapterSidebar 컴포넌트 삽입 (02-03 placeholder 교체)"
      pattern: "ChapterSidebar"
---

<objective>
챕터 사이드바를 완성한다. @dnd-kit 기반 드래그앤드롭 순서 변경, 챕터 상태 변경 드롭다운, 챕터 추가 버튼을 구현하고, 02-03에서 placeholder로 남긴 에디터 사이드바 자리에 실제 ChapterSidebar 컴포넌트를 연결한다. Fractional indexing 계산 로직(order-key.ts)도 이 플랜에서 구현한다.

Purpose: 챕터 관리의 핵심 인터랙션(순서 변경, 상태 관리, 챕터 이동)을 완성해 사용자가 소설 집필 흐름을 완전히 제어할 수 있게 한다.
Output: ChapterSidebar, ChapterSortableList 컴포넌트, order-key.ts 유틸, 에디터 라우트에 사이드바 연결.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-novel-chapter-management/02-CONTEXT.md
@.planning/phases/02-novel-chapter-management/02-RESEARCH.md
@.planning/phases/02-novel-chapter-management/02-01-SUMMARY.md
@.planning/phases/02-novel-chapter-management/02-02-SUMMARY.md
@.planning/phases/02-novel-chapter-management/02-03-SUMMARY.md

<interfaces>
<!-- Wave 2 완료 시점에 이미 존재해야 하는 계약 -->

apps/web/src/features/novel/types/novel.ts (02-02):
```typescript
export interface Chapter {
  id: string;
  novel_id: string;
  title: string;
  content: Record<string, unknown> | null;  // D-28: JSONB (getJSON() 결과)
  order_key: number;
  status: ChapterStatus;
  created_at: string;
  updated_at: string;
}
export type ChapterStatus = 'draft' | 'reviewing' | 'done';
```

apps/web/src/features/novel/hooks/use-chapter-mutations.ts (02-03):
```typescript
export function useReorderChapterMutation(): UseMutationResult<...>
export function useCreateChapterMutation(): UseMutationResult<...>
export function useUpdateChapterMutation(): UseMutationResult<...>
```

apps/web/src/features/novel/hooks/use-chapter-queries.ts (02-03):
```typescript
export function useChapters(novelId: string): UseQueryResult<Chapter[]>
```

@dnd-kit 패키지 (02-03에서 설치됨):
```typescript
import { DndContext, closestCenter, PointerSensor, KeyboardSensor, useSensor, useSensors, DragEndEvent } from '@dnd-kit/core';
import { SortableContext, sortableKeyboardCoordinates, verticalListSortingStrategy, useSortable, arrayMove } from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';
```

RESEARCH.md §3.1 calcOrderKey 알고리즘:
```typescript
const INITIAL_GAP = 1000;
// reordered: arrayMove 후 배열, targetIndex: 이동된 아이템 인덱스
function calcOrderKey(reordered: { order_key: number }[], targetIndex: number): number {
  const prev = reordered[targetIndex - 1]?.order_key;
  const next = reordered[targetIndex + 1]?.order_key;
  if (prev === undefined && next === undefined) return INITIAL_GAP;
  if (prev === undefined) return next! - INITIAL_GAP;
  if (next === undefined) return prev + INITIAL_GAP;
  return (prev + next) / 2;
}
```

02-03 에디터 라우트 사이드바 placeholder (교체 대상):
```tsx
// 현재: <div className="w-64">챕터 목록 로드 중...</div>
// 교체 후: <ChapterSidebar novelId={novelId} currentChapterId={chapterId} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fractional Indexing 유틸 + ChapterSortableList 컴포넌트</name>
  <files>
    apps/web/src/features/novel/lib/order-key.ts,
    apps/web/src/features/novel/components/chapter-sortable-list.tsx,
    apps/web/src/features/novel/components/chapter-status-badge.tsx
  </files>
  <behavior>
    - calcOrderKey(reordered, targetIndex): 맨 앞 삽입 → next - 1000, 맨 뒤 삽입 → prev + 1000, 중간 → (prev + next) / 2
    - needsReindex(chapters): 인접 order_key 차이가 0.001 미만이면 true
    - ChapterSortableList: DndContext + SortableContext로 수직 리스트 구현. 각 아이템에 드래그 핸들(⠿ 아이콘 또는 GripVertical lucide) 분리. 핸들에만 listeners 적용 (전체 아이템 클릭 vs 드래그 구분)
    - onDragEnd: arrayMove → calcOrderKey → needsReindex 체크 → onReorder 콜백 호출 (activeId, newOrderKey 전달)
    - ChapterStatusBadge: status별 색상 배지 (draft=회색, reviewing=노랑, done=초록)
  </behavior>
  <action>
    1. lib/order-key.ts 작성 — RESEARCH.md §3.1 알고리즘 그대로. INITIAL_GAP=1000, MIN_GAP=0.001. 함수: calcOrderKey, needsReindex (named export).

    2. features/novel/components/chapter-status-badge.tsx:
       - Props: status(ChapterStatus)
       - 배지: draft → "초안" gray, reviewing → "검토 중" yellow, done → "완성" green
       - 작은 크기 (text-xs, px-2, py-0.5, rounded-full)

    3. features/novel/components/chapter-sortable-list.tsx:
       - Props: chapters(Chapter[]), currentChapterId(string), novelId(string), onReorder((activeId: string, newOrderKey: number) => void), onChapterClick((chapter: Chapter) => void)
       - sensors: PointerSensor + KeyboardSensor(sortableKeyboardCoordinates)
       - 각 SortableChapterItem: useSortable({ id: chapter.id }), CSS.Transform.toString(transform), 드래그 핸들은 별도 div (listeners만 적용)
       - 현재 챕터(currentChapterId)는 강조 표시 (bg-primary/10 또는 border-l-2 border-primary)
       - 아이템 클릭: onChapterClick 호출
       - 드래그 핸들: GripVertical lucide-react 아이콘, cursor-grab
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep -E "error TS" | grep -E "order-key|sortable|status-badge" | head -10</automated>
  </verify>
  <done>
    - lib/order-key.ts 존재, calcOrderKey와 needsReindex export
    - chapter-sortable-list.tsx 존재, DndContext + SortableContext + useSortable 포함
    - chapter-status-badge.tsx 존재, 3가지 상태 색상 분기
    - pnpm typecheck에서 해당 파일 오류 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: ChapterSidebar 완성 + 에디터 라우트 연결</name>
  <files>
    apps/web/src/features/novel/components/chapter-sidebar.tsx,
    apps/web/src/routes/_authenticated/novels/$novelId/chapters/$chapterId/edit.tsx
  </files>
  <action>
    1. features/novel/components/chapter-sidebar.tsx:
       - Props: novelId(string), currentChapterId(string)
       - useChapters(novelId)로 챕터 목록 로드
       - useReorderChapterMutation()으로 순서 변경
       - useCreateChapterMutation()으로 챕터 추가
       - useUpdateChapterMutation()으로 상태 변경
       - useNavigate()로 챕터 이동

       레이아웃 (h-full, w-64, border-r, flex flex-col):
       - 상단: 소설 제목 또는 "챕터 목록" 헤더
       - 중단 (flex-1 overflow-y-auto): ChapterSortableList
         - onReorder: (activeId, newOrderKey) → reorderMutation.mutate({ novelId, chapterId: activeId, order_key: newOrderKey })
         - onChapterClick: navigate({ to: '/novels/$novelId/chapters/$chapterId/edit', params: { novelId, chapterId: chapter.id } })
       - 챕터 아이템 우측: ChapterStatusBadge + 상태 변경 select (드롭다운)
         select onChange → updateMutation.mutate({ novelId, chapterId, data: { status: newStatus } })
       - 하단 (border-t): "+ 챕터 추가" 버튼
         클릭 → createMutation.mutate({ novelId, data: { title: `챕터 ${chapters.length + 1}` } })
         onSuccess → navigate to new chapter edit page

       isLoading → 스피너, isError → "챕터를 불러올 수 없습니다"

    2. routes/_authenticated/novels/$novelId/chapters/$chapterId/edit.tsx 수정:
       - 02-03에서 추가한 placeholder div (className="w-64" > "챕터 목록 로드 중...") 를 ChapterSidebar 컴포넌트로 교체:
         <ChapterSidebar novelId={novelId} currentChapterId={chapterId} />
       - 나머지 코드는 건드리지 않음 (surgical change)

    needsReindex 처리: onReorder 핸들러에서 arrayMove 후 needsReindex(reordered) 검사. true이면 console.warn("order_key 재인덱싱 필요 — MVP 단계에서는 수동 대응") 로그만 출력 (D-29: 재인덱스 엔드포인트 없음).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck 2>&1 | grep -v "^$" | tail -20</automated>
  </verify>
  <done>
    - chapter-sidebar.tsx 존재, ChapterSortableList + 상태 변경 드롭다운 + 챕터 추가 버튼 포함
    - edit.tsx에서 placeholder div가 ChapterSidebar로 교체됨
    - pnpm typecheck 통과
    - pnpm build 오류 없음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (DnD) → API | 드래그 후 계산된 order_key 값을 PATCH로 전송 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | order_key 조작 (클라이언트 계산) | accept | order_key는 시각적 정렬용 — 소유권 검증은 서버 서비스 레이어에서 수행됨 (T-02-01~03으로 커버) |
| T-02-10 | DoS | 빠른 드래그 반복으로 다수 PATCH | mitigate | onDragEnd는 드롭 이벤트 1회당 1 PATCH — 연속 드래그는 시각적으로만 업데이트, 드롭 후 서버 호출 |
| T-02-SC | Tampering | @dnd-kit 패키지 | accept | RESEARCH.md §6에서 승인 완료 (02-03 설치 시점에 검증) |
</threat_model>

<verification>
```bash
# 타입 체크
cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm typecheck

# 빌드 확인
cd /Users/gyuha/workspace/storywriter-studio/apps/web && pnpm build 2>&1 | tail -10

# 핵심 파일 존재 확인
ls /Users/gyuha/workspace/storywriter-studio/apps/web/src/features/novel/lib/order-key.ts
ls /Users/gyuha/workspace/storywriter-studio/apps/web/src/features/novel/components/chapter-sidebar.tsx
ls /Users/gyuha/workspace/storywriter-studio/apps/web/src/features/novel/components/chapter-sortable-list.tsx

# calcOrderKey export 확인
grep -v '^//' /Users/gyuha/workspace/storywriter-studio/apps/web/src/features/novel/lib/order-key.ts | grep -c "export function"

# ChapterSidebar가 edit.tsx에 연결됐는지 확인
grep -n "ChapterSidebar" /Users/gyuha/workspace/storywriter-studio/apps/web/src/routes/_authenticated/novels/\$novelId/chapters/\$chapterId/edit.tsx
```
</verification>

<success_criteria>
- lib/order-key.ts: calcOrderKey, needsReindex 함수 export
- chapter-sortable-list.tsx: @dnd-kit DndContext + SortableContext + useSortable + arrayMove + calcOrderKey 연동
- chapter-status-badge.tsx: 3가지 상태 색상 배지
- chapter-sidebar.tsx: 챕터 목록 표시, 드래그앤드롭, 상태 변경 드롭다운, 챕터 추가 버튼
- edit.tsx: placeholder div → ChapterSidebar로 교체
- pnpm typecheck 통과
- pnpm build 오류 없음
- Phase 2 Success Criteria 4개 항목 모두 달성 가능한 상태
</success_criteria>

<output>
완료 후 `.planning/phases/02-novel-chapter-management/02-04-SUMMARY.md` 생성
</output>
