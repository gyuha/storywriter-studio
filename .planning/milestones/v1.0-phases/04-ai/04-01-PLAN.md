---
phase: "04-ai"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/main.py
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-04
  - AI-01
  - AI-02
  - AI-03
  - AI-04
  - AI-05

must_haves:
  truths:
    - "POST /api/v1/novels/{novel_id}/chapters/{chapter_id}/draft 엔드포인트가 FastAPI /docs에 노출된다"
    - "챕터 에디터 접근 시 우측 사이드패널(4탭)이 표시된다"
    - "세계관 항목 선택 후 AI 초안 생성 버튼 클릭 시 에디터에 텍스트가 스트리밍된다"
    - "pnpm typecheck가 오류 없이 통과한다"
  artifacts:
    - path: "apps/api/src/main.py"
      provides: "_register_routers()에 draft_router 등록 완료"
      contains: "draft_router"
  key_links:
    - from: "apps/api/src/main.py"
      to: "apps/api/src/domains/novel/router/draft_router.py"
      via: "_register_routers()의 include_router"
      pattern: "include_router.*draft_router"
    - from: "apps/web/src/features/novel/hooks/use-ai-draft.ts"
      to: "/api/v1/novels/{novel_id}/chapters/{chapter_id}/draft"
      via: "fetch() POST 요청"
      pattern: "fetch.*draft"
---

<objective>
Phase 4 완료 처리 — `draft_router`를 `main.py`에 등록하고, 이미 구현된 UI + SSE 클라이언트 + 백엔드가 실제로 연결되어 동작하는지 검증한다.

Purpose: 구현 99% 완료 상태의 Phase 4에서 유일하게 누락된 `draft_router` 등록(D-22)을 추가함으로써 AI 초안 생성 기능 전체를 활성화한다.
Output: 동작하는 AI 초안 생성 파이프라인 (에디터 사이드패널 → 세계관 컨텍스트 선택 → SSE 스트리밍 초안 생성).
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/storywriter-studio/.planning/PROJECT.md
@/Users/gyuha/workspace/storywriter-studio/.planning/ROADMAP.md
@/Users/gyuha/workspace/storywriter-studio/.planning/REQUIREMENTS.md
@/Users/gyuha/workspace/storywriter-studio/.planning/phases/04-ai/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: draft_router를 main.py에 등록</name>
  <files>apps/api/src/main.py</files>
  <action>
    `_register_routers()` 함수에 draft_router 등록 블록을 추가한다. 기존 world_router 블록(Chat domain 블록 바로 앞) 다음에 아래 패턴과 동일하게 삽입한다 (per D-22):

    - import 경로: `domains.novel.router.draft_router`
    - router 변수명: `draft_router`
    - prefix: `"/api/v1/novels"` (엔드포인트가 `/{novel_id}/chapters/{chapter_id}/draft`이므로 최종 URL이 `/api/v1/novels/{novel_id}/chapters/{chapter_id}/draft`가 된다)
    - tags: draft_router 자체에 `tags=["draft"]`가 이미 선언되어 있으므로 별도 지정 불필요
    - logger.debug 호출: `logger.debug("router_registered", prefix="/api/v1/novels/{novel_id}/chapters")`
    - try/except ImportError 블록으로 감싸야 한다 (기존 패턴 동일)

    추가 위치는 world_router 블록과 chat_router 블록 사이.
    기존 코드 스타일 그대로 유지한다. 다른 라우터 블록은 수정하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/storywriter-studio/apps/api && grep -n "include_router.*draft_router\|draft_router.*novels" src/main.py</automated>
  </verify>
  <done>
    `src/main.py`에 draft_router import와 include_router 호출이 존재한다.
    서버 재시작 후 `/docs`에서 `POST /api/v1/novels/{novel_id}/chapters/{chapter_id}/draft` 엔드포인트가 노출된다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    1. main.py에 draft_router 등록 (Task 1)
    2. 기존 구현: editor-layout.tsx (2패널), editor-right-panel.tsx (4탭), use-ai-draft.ts (SSE 클라이언트)
  </what-built>
  <how-to-verify>
    **Step 1 — 백엔드 엔드포인트 노출 확인**
    ```
    cd apps/api && uv run uvicorn src.main:app --reload
    ```
    브라우저에서 http://localhost:8000/docs 접속 후 "draft" 태그 아래
    `POST /api/v1/novels/{novel_id}/chapters/{chapter_id}/draft`가 표시되는지 확인.

    **Step 2 — 프론트엔드 사이드패널 확인**
    ```
    cd apps/web && pnpm dev
    ```
    http://localhost:3000 에서 로그인 후 소설 1개 + 챕터 1개를 생성하고 챕터 에디터로 진입.
    우측에 사이드패널(캐릭터 / 배경/설정 / 교정 / 채팅 탭)이 표시되는지 확인.

    **Step 3 — AI 초안 생성 E2E 확인**
    사이드패널의 "배경/설정" 탭에서 세계관 항목 1개 이상 체크.
    에디터 하단(또는 패널 내) "AI 초안 생성" 버튼 클릭.
    에디터에 텍스트가 스트리밍되어 입력되는지 확인.
    생성 중 "취소" 버튼 클릭 시 스트리밍이 멈추는지 확인.

    **Step 4 — 타입 검사**
    ```
    cd apps/web && pnpm typecheck
    ```
    오류 없이 완료되는지 확인.

    **Step 5 — openapi.json/generated 동기화 확인**
    ```
    cd apps/web && pnpm generate:api
    git diff --name-only
    ```
    변경된 파일이 없으면 현재 `openapi.json`이 이미 올바른 상태. 변경이 있으면 변경된 파일을 확인하고 커밋에 포함.

    **참고:** LLM API 키가 없는 환경에서는 Step 3에서 500 에러가 발생할 수 있음.
    이 경우 `/docs`에서 직접 호출해 요청이 라우터까지 도달하는지(401 또는 422)만 확인해도 됨.
  </how-to-verify>
  <resume-signal>4가지 확인 결과를 알려주세요. 문제가 있다면 어떤 단계에서 발생했는지 설명해주세요. 모두 정상이면 "approved" 입력.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → draft API | 인증된 사용자의 POST 요청. novel_id/chapter_id 소유권 검증 필요. |
| draft API → LLM provider | 서버 사이드 외부 API 호출. API 키는 환경변수로만 관리. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | `generate_draft` 엔드포인트 | mitigate | `get_current_user` Depends로 JWT 인증 이미 적용. novel 소유권 검증 (`novel.user_id != current_user.id`) 이미 구현. |
| T-04-02 | Information Disclosure | SSE 스트리밍 응답 | accept | HTTPS 전송 전제. 인증 없는 접근은 401 반환. |
| T-04-03 | Denial of Service | LLM 스트리밍 long-running 요청 | accept | MVP 단계. 사용자당 rate limit은 slowapi로 적용 가능. v2 개선 항목. |
| T-04-SC | Tampering | npm/pip 패키지 설치 없음 | accept | 이 Plan은 신규 패키지를 설치하지 않음. main.py 2줄 수정만 포함. |
</threat_model>

<verification>
## Phase 4 최종 검증

1. `grep -n "draft_router" apps/api/src/main.py` — 2줄 이상 출력 (import + include_router)
2. FastAPI `/docs`에서 `POST /api/v1/novels/{novel_id}/chapters/{chapter_id}/draft` 노출 확인
3. 챕터 에디터 접근 시 우측 사이드패널 4탭 표시
4. AI 초안 생성 버튼 → SSE 스트리밍 → 에디터에 텍스트 삽입
5. `pnpm typecheck` 오류 없음
</verification>

<success_criteria>
1. `POST /api/v1/novels/{novel_id}/chapters/{chapter_id}/draft`가 FastAPI `/docs`에 노출된다
2. 브라우저에서 챕터 에디터 접근 시 우측 사이드패널이 표시된다
3. 세계관 항목을 선택하고 "AI 초안 생성" 버튼 클릭 시 에디터에 텍스트가 스트리밍된다
4. `pnpm typecheck`가 오류 없이 통과한다
</success_criteria>

<output>
완료 시 `.planning/phases/04-ai/04-01-SUMMARY.md` 생성
</output>
